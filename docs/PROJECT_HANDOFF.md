# 프로젝트 핸드오프

> 새 세션이나 새 환경에서 이 프로젝트를 이어받을 때 읽는 문서입니다.
> **"뭐 해야 함?" 이라는 질문의 답은 아래 「다음에 할 일」 절에 있습니다.**

**최종 갱신:** 2026-08-31 (★ **Phase 4 G6 착수 — 적 2종 구현(IMPL-006)**. `Gameplay/Enemies/EnemyAI.cs` 하나로 야생 생물(순찰·영역 리시)·붕괴자(휴면 허스크·리시無)를 직렬화 값만으로 가름. enum FSM(Patrol/Chase/Attack/Dead), 전이표는 순수 함수 `NextState`. **전투는 CombatCore 재사용, 신규 전투 코드 0**. ⚠️ **PlayMaker 대체 임시 구현** — 구매 전까지 C# FSM으로 대신(사용자 2026-08-31 결정), PlayMaker 이식 시 `NextState` 상태표를 그대로 옮긴다. Player_A에 여태 없던 Health(5)+Hurtbox(L10)+DamageResponse 배선(적 타격 대상). 테스트 **EditMode 54/54**(+6). 플레이 실측 전 흐름 통과. **다음은 G7 균형자 기습 → G8 스위치 암전 → G9 데모 조립**)

이전: 2026-08-28 Phase 3 완료(B 동행 정식화, 72/72). 2026-08-27 Phase 1·2 완료(전투·메트로배니아 뼈대, 66/66).

---

## 다음에 할 일 ★

**8/27 — 구현 로드맵 확정. 코드는 이 순서로 간다.** 근거: `specs/2026-08-19-implementation-core-architecture-design.md`의 5단계 세로 슬라이스를 현재 진척(1차 완료·2차 절반)에 맞춰 재편한 것. 완주하면 「여관→놀이→기습→B가 스위치를 다시 누름→암전」 데모 세로 슬라이스가 완성된다.

- **Phase 1 — 전투 뼈대 완성 (C7 마무리 + G2)** ✅ **완료 (2026-08-27, IMPL 9차)**
  - Hitbox(쿼리 기반)·콜리전 매트릭스(9·10층 물리 접촉 차단)·자기피해 가드(편+owner 루트 2중)·테스트 49/49
  - 공격은 FSM 상태가 아니라 **병행 컴포넌트**(이동 잠금 = 응답 지연이라 금지) — 무게는 히트스톱(국소 정지)이 진다
  - 게임필 §9 합류: `DamageInfo.Strong` + `HitSignal`(초크포인트=Health) + DamageResponse(플래시·넉백) 완비 — 적 생기면 자동 점등
  - 남은 아트 부채: A Attack/Hurt 애니(현재 그레이박스 스윙 이펙트)
- **Phase 2 — 메트로배니아 뼈대 (C8 + 카메라 + C4 + C5 + G3)** ✅ **완료 (2026-08-27, IMPL 10차)**
  - **카메라가 드디어 움직인다** — `CameraFollower`(룩어헤드 1.3 + SmoothDamp 0.16 + 룸 클램프 + 강공격 흔들림 §9). 패럴랙스가 실플레이에서 보인다
  - C8 `Room`/`RoomTracker`(진입·전환 신호) / C4 `ProgressionState` 3상태 + `AbilityChangedSignal` / C5 `SaveSystem` JSON(체크포인트 룸ID 필드 예약) / G3 `PlayerDash`(Dashing 상태, 공중 1회, 해금 흐름 = Pickup_F1 → E → Shift 실측)
  - ⚠️ **이 프로젝트는 Enter Play Mode 도메인 리로드가 꺼져 있다** — static이 플레이 간에 살아남아 F1 해금이 다음 플레이에 새는 것을 실측으로 발견. `ProgressionState`·`EventBus`에 `RuntimeInitializeOnLoadMethod(SubsystemRegistration)` 리셋으로 청산. **앞으로 Core에 static 상태를 추가하면 같은 리셋을 달 것**
  - 미구현으로 명시: 재화(데모에 콘텐츠 없음), 체크포인트 오브젝트(G9 데모 조립 때 — 세이브 포맷엔 자리 있음), 룸 전환 카메라 연출(룸이 화면보다 크면 클램프 점프 — 실룸 설계 때)
- **Phase 3 — B 동행 정식화 (G4 승격 + G5 F2 받침)** ✅ **완료 (2026-08-28, IMPL 11차)**
  - G4: `CompanionFollower`가 C3 StateMachine FSM 3상태(Following/Snapping/Cooperating). `EventBus.Subscribe<BoostRequestedSignal>` — 신호 반응(명령 큐 아님). 뷰(애니/턴/facing/sleep)는 상태 무관 공통
  - G5: `PlayerBoost`(F2, PlayerDash 패턴). 공중 재점프 → `IsUsable("F2")`면 위로 밀어올림 + B가 밑으로 파고듦(Cooperating). **미획득/잠김이면 무시 = 「A는 스스로 높이를 얻지 못한다」 성립**(테스트 3건이 못 박음)
  - 씬: 두 씬 Player_A에 PlayerBoost, Movement에 Pickup_F2 @(-8,0.35) — Ledge_TooHigh 게이트 시연. 재로드 검증 완료
  - 남은 것: **결별 트리거(F2→Locked)는 G8/데모 조립 때** · B 받침 포즈 아트(현재 코드 스냅만) · 라이브 실측(로직은 테스트 커버)
- **Phase 4 — 적·균형자·데모 조립 (G6→G7→G8→G9)**
  - **G6 적 2종** ✅ **완료 (2026-08-31, IMPL-006)** — `Gameplay/Enemies/EnemyAI.cs`(enum FSM, 야생·붕괴자 = 값 차이). CombatCore 재사용(신규 전투 코드 0). 「Damageable 공용 베이스」 부채는 **이미 `Health`가 유일 구현이라 청산돼 있었음**(세 번째 사본 없음). ⚠️ **PlayMaker 대체 임시 C# FSM** — 구매 시 `NextState` 상태표 이식 / Player_A에 Health+Hurtbox+DamageResponse 신규 배선(HP 0 죽음·리스폰·UI는 **G9 몫**). EditMode 54/54 + 플레이 실측
  - ← **여기부터**: G7 균형자는 데모용 스크립트 기습 1회만 / G8 스위치 연출+암전 / G9 데모 룸 그레이박스(여관→심부름→튜토리얼·놀이→기습) + **플레이어 HP 0 처리(죽음/암전)**
- **횡단 트랙**: ① **연출 층 0개**(발소리·구동음·애니 관성·배기 파티클 — A가 「기계」로 읽히려면 필요, Phase 1~2 사이 1회분 권장) ② 아트 부채(B sleep 전용·A turn 네이티브 64px·B walk/fall 턴)
- **지킬 규칙**: 조작감 튜닝 전 응답/무게 층 판정(`jumpHeight 1.7` 불가침) · 튜닝 값은 C# + 두 씬 직렬화 동반 갱신 · 빌더 씬은 저장 후 재열기 검증 · >1파일/>200줄이면 IMPL 선등록 · 데모 밖 금지(F3~F5·그릇 UI·전생 기억·맵 실설계·붕괴 페널티)

**8/24 — A·B 애니메이션 대량 반입. 게임이 훨씬 살아있어졌다.** 상세는 `IMPL_REGISTRY.md` 8차 · `ART_LOG.md` 2026-08-24.
- **A 64px 애니 전면 반입** — idle에 이어 **run(6f)·jump(3f)·fall(6f)·turn(45°/정면)**을 전부 64px/PPU 64로 교체. 방식은 idle과 동일(.meta를 64px 단일 스프라이트로 재작성, GUID 유지 → 클립이 자동으로 새 그림을 가리킴). `A_run_6/7` 삭제, `A_Run.anim`을 6프레임으로 재작성
- ★ **fall은 「거리비례 틸트」 코드로 구동** — `PlayerAnimator.ApplyFallTilt()` 신설. 정점부터 잰 하강거리를 `fallForMaxTilt`(현재 **1.5u**)로 정규화해 `A_fall_0`(직립)→`A_fall_5`(다이빙) 프레임을 고른다. **물리 불간섭**(LateUpdate 덮어쓰기, turn과 같은 패턴). 값이 크면 안 기울어 보이므로 4→2→1.5로 낮춤(일반 점프 1.7u 낙하가 fall_4까지 도달). 두 씬 Player_A에 배선
- ⚠️ **turn은 임시 품질** — 네이티브 64px 프레임이 없어 업스케일 프리뷰 시트(`A_turn_frames.png`)에서 idx2/idx6을 flood-fill 투명화 후 다운스케일해 뽑음. 회전 0.14초 동안만 보이므로 수용. 깔끔히 하려면 PixelLab 네이티브 재생성 필요
- **B jump/fall 신규** — PixelLab `animate_with_text_v3`로 생성(각 1생성). 1차 초안은 둘 다 idle성 흔들림이라 구분 안 돼 **과장·6프레임으로 재생성**(정점서 40%+ 차이 확인). `B_Jump.anim`(비루프)·`B_Fall.anim`(루프) + `B_Animator`에 Jump/Fall 상태·전이·파라미터(Grounded·VSpeed) 추가
- ★ **B 공중 상태는 A 기준** — B는 A 높이를 따라다니므로 `CompanionFollower`가 **A의 실제 세로속도(`target.Velocity.y`)**로 VSpeed 구동. B의 SmoothDamp 지연속도로 넣으면 fall이 늦게·짧게 떠서 안 보인다
- 🐛 ★ **「A 착지 = B 착지」 버그 수정** — B 접지를 A 접지로 넣으면 A가 먼저 닿을 때 B가 공중에서 착지 판정돼 fall이 끊긴다. **`bGrounded = A접지 && (B가 A높이 0.06u 이내로 내려옴)`** 으로 B 자신의 위치 기준 판정. B는 실제로 내려앉을 때까지 fall 유지
- **애니 반입 에디터 툴 신설** — `Assets/Scripts/Editor/CharacterAnimationTool.cs`, 메뉴 `Miji ▸ Animation ▸ Character Animation Tool`. 외부 폴더(docs/art/assets…) 프레임을 원클릭으로 캐릭터 Sprites에 반입 + .anim 클립 생성/제자리 갱신(PPU 64·Point·무압축·GUID 보존). A·B 공용
- ⚠️ **B는 sleep 전용 아트가 없어** `B_Sleep.anim`을 idle 프레임으로 임시 재지정(죽은 참조였던 것 복구). 전용 sleep 포즈는 추후
- ⚠️ **PixelLab MCP 이번 세션엔 붙어 있었다** — Tier 2, 잔여 ~4,325. `.claude/settings.local.json`에 `mcp__pixellab-forge__*` 허용 규칙 추가(auto-mode 분류기 차단 해소)
- 검증: compile 0/0 · **EditMode 25 + PlayMode 8 = 33/33** · uloop 플레이 실측(fall 틸트·B jump/fall 전이 전부 확인)

8/4에 여행의 구조, **A·B 갈등축**, **B의 성격**, **플레이어 시점**이 확정됐다.
이야기의 뼈대는 이제 처음부터 재회까지 이어져 있고, 남은 것은 결말과 **세계의 물리적 형태**다.

8/5에 산문 초고를 완독하고 **결말 검토를 시작했으나 결론이 나지 않았다.** 후보 3종과 각각의 문제까지 정리되어 있으므로 「결말 검토」 절에서 이어가면 된다. 같은 날 **말하기=주기 메카닉** 안이 나왔다(전용 절 참조).

**8/8 — 「이동 능력의 서사적 당위성」에서 시작해 「이원 무브셋」 설계로 번졌다.** 사용자 지적("2단 점프는 B가 밀어주는 식")이 갈등 스펙 5절의 **「결별 = 동행자 시스템 상실」에 구현 수단이 없다는 공백**과 맞물렸다. **1~4절 승인, 5~7절 미승인 상태로 중단.** 「이원 무브셋」 절에서 5절(데모 스코프 변경)부터 이어가면 된다. 승인되면 `specs/2026-08-08-*-design.md` 작성 + `DECISIONS.md` 기록 + `MECHANIC_movement.md` 개정이 남는다.

**8/11 — 「로봇이 왜 있는가」에서 시작해 균형자를 통째로 재설계했다.** 전설의 빈칸(누가 누구와, 무슨 수단으로)이 채워졌고 그 대가로 **확정 4건이 뒤집혔다.** 전용 절 참조. **✅ 2026-08-11 기록 완료** — `DECISIONS.md`에 뒤집힌 4건 + 결정 1·2 기록, 스펙 `specs/2026-08-11-balancer-origin-design.md` 작성, 점화 스펙 5절 개정. **남은 것은 2장 재설계와 산문 9건 승인**(전용 절 참조).

**8/19(코드) — 실제 구현 시작. 게임이 움직인다.** IMPL-005로 **Core 1차 뼈대**(EventBus·InputReader/Router·StateMachine·GameFlow) + **G1 A 컨트롤러** + **조작감 관용 기법**을 구현했다. `Assets/Scenes/Greybox_Movement.unity`에서 A가 걷고 뛴다. **테스트 27/27**(EditMode 19 + PlayMode 8). 상세는 `docs/agents/IMPL_REGISTRY.md` IMPL-005.
- ★ **asmdef로 「Gameplay → Core」 단방향 의존을 컴파일러가 강제한다** — Core에서 스토리 명사를 쓰면 빌드 에러. 문서 규칙이 아니라 빌드 규칙
- ★ **F2 게이트가 지형으로 성립함을 수치로 확인** — Ledge_TooHigh(3.65)는 어디서도 단일 점프로 못 닿음. 「A는 스스로 높이를 얻지 못한다」가 코드·지형 양쪽에서 강제된다(2단 점프는 코드에 아예 없음)
- **조작감**: 코요테 0.09 / 점프버퍼 0.11 / **코너 보정 0.22**(HK식, 천장 모서리 통과) / **에이펙스 조정**(정점만 중력 0.65·제어 1.45) / **반전 스냅 2.1** — 기준은 「무게를 줄이지 않고 '눌렀는데 안 됐다'만 없앤다」
- 🐛 점프 직후 코요테 재충전으로 **이중 임펄스** 가능했던 버그 발견·차단(`jumpLockout` 0.08)
- ⚠️ **PlayMode 테스트 asmdef는 `includePlatforms: []`** 여야 한다(Editor 전용이면 EditMode로 돌아 물리 미작동 → 전부 실패)
- **다음**: C7 CombatCore(Hitbox/Hurtbox/IDamageable) → G2 근접공격·상호작용

**8/20 — A가 얼굴을 얻었다.** 그레이박스 사각형이던 Player_A를 **PixelLab MCP로 생성한 32x32 픽셀 스프라이트 + 애니메이션 4종**(Idle/Run/Jump/Fall)으로 교체했다. 기준 컨셉은 `character_01_A_switch_robot`(스위치·시안 렌즈·이끼 몸통·롤러 전부 32px 생존). `PlayerAnimator.cs`(뷰 전용) 신설, 콜라이더를 그림 몸통에 정합(스위치는 충돌 제외). 상세는 `ART_LOG.md` 2026-08-20 + `IMPL_REGISTRY.md` 3차.
- ✅ **스프라이트 32x32 이탈 건은 2026-08-20 해소** — AK-xolotl: Together를 밀도 레퍼런스로 채택하며 **캐릭터 기본 32x32 / 타일 16x16 / PPU 32**로 스타일 가이드 공식 개정(밀도만 참고, 톤·IP 복제 금지). `DECISIONS.md`·`ART_LOG.md` 5차
- ⚠️ **PixelLab 트라이얼 잔여 3생성** — 저비용 파이프라인(pixen 1장 + animate_image 파생, 회당 1생성)은 검증됨. Attack/Hurt 애니는 G2 때 필요
- **C7 CombatCore는 여전히 중단 상태** — Damage/Health/Hurtbox 3파일 작성됨(미커밋이었다가 이번 커밋에 포함), **Hitbox 미작성·테스트 없음·레지스트리 정식 등록 전.** 여기부터 이어가면 된다

**8/20(후반) — 아트 파이프라인이 섰다.** ① **동굴 타일 2종 적용**(지반·발판·벽 전부 Tiled 렌더 전환, 콜라이더 월드 크기 불변) — PixelLab이 심리스 타일에 실패해서(pixen은 "seamless" 지시를 무시하고 낱개 블록을 그림, 2생성 낭비) **A 팔레트 샘플링 수제 도트**로 제작. ② **B(무리비) 테스트 추종** — 마지막 1생성으로 스프라이트 1장(이마 균열 없음 = 시작 상태), `CompanionFollower.cs`(입력·콜라이더 없음, 처지면 스냅 복귀, 3절 뼈대 준수). ③ 사용자 지적 「B가 2D 같다」 → **릴라이트 파이프라인 개발**(`relight_b.ps1` — 팔레트를 A 올리브 램프로 양자화 + 엣지 릴라이트 + 눈 수술). **재사용 가능 — 생성물 톤 통일의 표준 통로.** ④ **sprite-gen 도입**(`tools/`, 세팅 스크립트 커밋, 리포 자체는 gitignore) — 분업: PixelLab=플레이 레이어 실기, sprite-gen(Codex)=컨셉·배경. `DECISIONS.md` 2026-08-20 행. ⑤ ⚠️ **PixelLab 트라이얼 소진(잔여 0)** — 추가 생성은 Tier 2 구독 필요(월 ~$24, 전체 맵 아트 1~2개월 커버 추산). 상세는 `ART_LOG.md` 2026-08-20 2~4차.

**8/20(Tier 2) — 아트가 프로덕션 품질로 올라섰다.** PixelLab **Tier 2 「Pixel Artisan」 구독**(월 5,000생성) 개시. 이번 세션 누적 **293생성 사용, 잔여 ~4,707.** 상세는 `ART_LOG.md` 6~11차.
- **A·B 프로 재생성** — `create_image_pro` 후보 64장 풀에서 선별. ★ **교훈: pro는 레퍼런스를 걸어도 정체성 요소(A 스위치·렌즈)를 후보 절반에서 빼먹고, 32px에서도 프롬프트에 없는 소품(우산·책·무기)을 붙인다 → 후보 선별이 필수 공정**
- **첫 배경** `BG_WovenNest.png` — 구조는 채택된 2-C 엮은 둥지, **색감만** `tech_level_02`(㉯는 기술수준으로는 탈락이나 팔레트 차용은 설정 무충돌). `ParallaxLayer.cs` 신설(카메라 추적 대비)
- **B 애니 3종** — Idle/Walk/**Sleep**(7초 정지 시 잠들고 움직이면 깸, 베이스는 사용자 지정 `B34_13`)
- **3프레임 턴** `Gameplay/View/TurnView.cs` — 좌우 반전 대신 **45°(출발) → 정면 → 45°(도착)**. A·B 공용, Animator 상태 증설 없이 LateUpdate 덮어쓰기
- ★ **후처리보다 생성이 싸다** — 트라이얼 시절 릴라이트 양자화가 B의 붉은 빛·단색 얼굴·A와의 품질 격차를 만들었다. Tier 2에서는 **`style_image`로 측면 스프라이트의 팔레트·명암을 복사해 재생성**하는 쪽이 정답. 릴라이트(`tools/relight_b.ps1`)는 톤 보정 전용으로 강등
- ★ **선별 기준에 실루엣 치수 추가** — 회전 프레임은 폭·발높이가 어긋나면 크기가 출렁인다. bbox 실측으로 맞추고 1px 시프트로 정렬(A 26 고정 / B 24·22·24, 발 위치 전부 31)
- **보류**: 점프 애니메이션 개선(사용자 지시 — 마지막에 제작 후 컨펌), PixelLab **캐릭터 회전 기능(v3 8방향)** 시험 중 — 성공하면 수동 조합을 대체
- **사용자 작업(별도)**: 균형자 컨셉 **변형 15종 생성 + selected/rejected 분류**, `generated-concepts/`를 characters·maps·world 서브폴더로 재편(+README). `DECISIONS.md`에 **균형자 비주얼 가드레일**(이름 직역 금지 — 저울·추·집게 폐기, 역할 기반 시각화) 기록

**8/20(구조) — 구조 감사 + 폴더 규칙 확정. 규칙 문서 `docs/agents/PROJECT_STRUCTURE.md` 신설.** asmdef 위반은 0건이었으나(단방향·네임스페이스·PlayMode 플랫폼 전부 정상) **구멍 3개**를 막았다. 테스트 **27 → 33** (EditMode 25 + PlayMode 8).
- ★ **`autoReferenced: true`가 단방향 규칙의 우회로였다** — `Assets/` 아무 데나 .cs를 떨구면 Assembly-CSharp에 들어가 Core·Gameplay를 **양쪽 다** 참조할 수 있었다. 「컴파일러가 강제한다」가 asmdef 밖에서는 거짓이었던 것. Core·Gameplay 둘 다 `false`로 바꿔 **모든 스크립트가 asmdef 아래에 있도록 강제**
- **`Miji.Gameplay.Tests`(EditMode) 신설** — 기존 EditMode가 Core 전용이라 `TurnView` 같은 **Gameplay의 순수 계산**을 잴 자리가 없어 PlayMode로 밀리고 있었다. `TurnViewTests` 6개 작성(3구간 분할·flipX·45° 폴백)
- **`StartupPossession.cs` → `Gameplay/Bootstrap/`** (유일하게 서브폴더가 없던 스크립트). 네임스페이스와 씬의 `m_EditorClassIdentifier`까지 동반 갱신
- ★ **아트는 역할이 아니라 정체성으로 가른다** — `Art/Player/`(A·B 혼재) → `Art/Characters/{A,B}/{Sprites,Animations}`. **2장에서 조작권이 인계되므로** A는 1·3장에서만 플레이어다. 역할로 가르면 확정 설계가 폴더를 깨뜨린다. **스크립트는 반대로 역할로** 가른다(`Player/`·`Companion/`) — 층이 다르니 기준이 달라도 된다. 배경·타일은 `Art/Environment/`로
- ★ **`src/miji`(디스크) vs `src/Miji`(git 인덱스) 대소문자 불일치 해소** — 사용자가 디스크를 대문자로 개명. 방치했으면 이날 만든 미추적 `.meta`들이 **소문자 경로로 커밋되어 리포 트리가 두 갈래로 쪼개질** 뻔했다. ⚠️ **역사 문서의 `src/miji/` 표기는 손대지 말 것** — 삭제된 Godot 프로젝트의 실제 경로다
- **`SampleScene.unity` 삭제**(사용자 지시). 빌드 세팅을 `Scenes/Greybox/Greybox_Movement.unity`로 교체 — 그냥 비우면 씬 없는 빌드가 나온다. 데모 씬이 생기면 재교체
- ⚠️ **개명·대규모 자산 이동 뒤 첫 `uloop launch`는 타임아웃이 난다** — `Library/` 전체 재빌드가 180초를 넘긴다. 고장이 아니므로 Unity 프로세스가 살아 있으면 잠시 뒤 명령을 다시 쏘면 된다. ULoop CLI는 전역 설치가 빠져 있어도 **`npx --yes uloop-cli@2.2.0`** 으로 부르면 된다
- ⚠️ **Unity 기동 시 ULoop이 `.claude/skills/` 33개를 CRLF로 다시 쓴다**(내용 동일). 매번 diff에 뜨므로 신경 쓰이면 `.gitattributes`로 개행 고정이 근본 해결
- **미해결로 남긴 것**: `Assets/` 루트의 URP 설정 3종은 옮기면 ProjectSettings 경로 참조가 깨질 수 있어 **이득 대비 위험이 커서 두기로 결정**

**8/21 — Codex 타일셋이 게임 안에 깔렸고, B 컨셉이 한 폴더로 정리됐다.** 두 갈래 작업이다.

**① WovenNest 타일셋 인게임 배치.** Codex가 만든 16px 타일 31종을 실제 Unity Tilemap으로 깔았다. `Assets/Scenes/Greybox/Greybox_WovenNest.unity` (Greybox_Movement 복제 → 그레이박스 블록 제거, A·B·카메라·배경 유지). 4레이어(BackWall −60 / Terrain −10 / Platforms −9 / Deco −5), 셀 0.5u, Terrain·Platforms에 `TilemapCollider2D + CompositeCollider2D`(레이어 Ground=6). **바닥 윗면 y=0으로 맞춰 그레이박스와 지면 높이가 같다.** 다리 높이 1.0 / 2.0(도달) / 4.0(단일 점프 불가, F2 게이트 시연). EditMode 25/25 통과.
- ★ **에디터 어셈블리 `Miji.Editor` 첫 입주** — `Assets/Scripts/Editor/WovenNestSampleRoomBuilder.cs`. 메뉴 `Miji/Tilemap/Woven Nest 예시 룸 빌드`. **도면은 아스키 txt**(`Art/Environment/Tiles/WovenNest/SampleRoom/SampleRoom_{Terrain,Deco}.txt`, 44x26) — txt만 고쳐 메뉴를 다시 누르면 방이 새로 그려진다.
- ⚠️ **`EditorSceneManager.OpenScene` 은 미사용 에셋을 언로드한다.** 씬을 열기 **전에** 읽어둔 Tile 에셋은 네이티브 객체가 파괴돼 `SetTile` 이 조용히 무시된다(C# 참조는 살아 있어 null 체크로도 안 잡힌다). **에셋 로드는 반드시 OpenScene 뒤에.** 이거 찾는 데 오래 걸렸다.
- **타일셋 소감(다음 아트 작업 입력)**: 뒷벽과 지반 명도가 같아 바닥이 안 단단해 보인다 / 뒷벽 변주가 벽지처럼 반복된다 / 랜턴은 Light2D 없이는 안 보인다 / RootArch는 지면에 놓으면 민둥 언덕으로 읽힌다. 잘 읽히는 건 뿌리다리와 GroundTop 잔풀.
- ⚠️ **Companion_B의 스프라이트가 비어 있다** — 8/20 정리에서 `Art/Characters/B/Sprites/*.png` 를 지운 결과다. 원본 `Greybox_Movement.unity` 도 마찬가지. 새 B 스프라이트를 물려야 한다.

**② B 컨셉 정리 — `docs/art/assets/b-current/poses/` 에 `B01`~`B15` 15종.** 흩어져 있던 `split`·`split-ears-unified`·`pose-sheet-64` 를 전부 한 폴더로 통합하고 사용자 선별을 거쳤다. 합본 아트워크 `B_artwork_all.png`.
- **확정 4종**: B01 Idle · B02 앉기 · B03 앉기+슬픔 · B04 물음표. 나머지 11종은 보류. **`fall` 에 배정된 그림이 없다.**
- ★ **눈 규격을 확정·문서화했다**(`b-current/README.md` 3절) — **검은 구체 + 크림 하이라이트 + 갈색 크레센트, 눈 하나 9x10px.** 기준은 `B02_sit`. 크기까지 규격이다. B01은 눈이 7x7 탁한 갈색 덩어리였고, **기준 파일의 눈 블록을 좌표만 맞춰 통째로 이식**해 고쳤다(손으로 키우는 것보다 안전 — 크기·색·구조가 자동 일치).
- **higgsfield로 포즈 시트 생성**(무료 플랜, 2회 4크레딧, **잔여 1.35 = 추가 생성 불가**). PixelLab MCP가 이 세션엔 없었다. 생성 결과 9종 중 4종이 온모델 이탈, 2종은 프레임 밖으로 잘려 폐기.
- ★ **`STYLE_GUIDE.md` 「생성 프롬프트 금지 사항」 신설** — **평면적으로 보이는 각도를 요청하지 않는다(3/4 앵글 기본)** 가 핵심. 그 외: 흰자위 금지를 써도 모델은 「놀람」을 흰자위로 그리므로 **표정 극단값은 수작업 수정**, 소품 금지해도 받침대를 발명함, 칸 여백 지시는 안 지켜짐.
- **후처리 도구 `tools/sheet-to-sprites.ps1` 신설** — 생성 시트를 **최빈색(mode) 샘플링**으로 64x64 낱장으로 내린다. 평균 다운스케일과 달리 평면 색과 1px 아웃라인이 안 뭉개진다.
- ⚠️ **한글 주석이 든 `.ps1` 은 UTF-8 **BOM**으로 저장해야 한다.** BOM이 없으면 PowerShell 5.1이 CP949로 읽어 주석이 깨지고 **깨진 주석이 다음 코드 줄을 삼킨다.** 문법 오류가 안 나고 조용히 오작동한다.
- ⚠️ **64x64는 스타일 가이드상 보스 크기다.** 이 컨셉들을 그대로 인게임에 넣으면 PPU 32에서 B가 2유닛(A의 두 배)이 된다. **인게임용 32x32 재작업이 별도로 필요하다.**
- **남은 불균질**: B01만 구 split 판본이라 귀가 갈라져 있고, B10~B15는 생성본이라 손그림 9종과 화풍이 다르다.

**8/21(후반) — 배경이 처음으로 화면에 나왔다.** Codex(sprite-gen)의 WovenNest **레이어 패스 01**(688x384 PNG 7장, 원경 안개 → 근경 뿌리)을 `Greybox_WovenNest.unity` 에 패럴랙스 스택으로 깔았다. 상세는 `IMPL_REGISTRY.md` 6차 · `ART_LOG.md` 2026-08-21.
- ★ **뒷벽 타일맵이 배경을 통째로 가리고 있었다** — `Tilemap_BackWall`(order −60, 불투명)이 방 내부를 다 덮어서 **`BG_WovenNest.png` 는 한 번도 화면에 나온 적이 없었다.** 8/21 오전의 「타일셋 소감」(뒷벽이 벽지처럼 반복된다 등)은 사실 **배경을 못 본 상태의 감상**이었다. 이제 빌더가 이 렌더러를 끈다 — 뿌리다리 틈으로 「새어 보이는 것」이 이제는 배경이라 막을 이유가 없다
- **`Assets/Scripts/Editor/WovenNestParallaxBuilder.cs`** 신설 — 메뉴 `Miji/Background/Woven Nest 패럴랙스 배경 구성`. 레이어 표(정렬순서·추종계수·틴트·on/off)가 코드에 있고 씬을 매번 새로 만든다. `Miji.Editor` asmdef 에 `Miji.Core, Miji.Gameplay` 참조 추가(런타임 컴포넌트를 붙이려면 필요 — 방향은 Editor → Gameplay → Core 로 단방향 유지)
- ★ **배경 배율은 1을 벗어나지 않는다** — 688x384 / PPU 32 = 21.5 x 12u 이고 카메라(ortho 6, 16:9)가 21.33 x 12u 라 **배율 1에서 캔버스 = 화면**이다. 이전 한 장짜리 배경은 1.5였는데, 그러면 배경 픽셀이 16px 타일보다 굵어져 한 화면에 픽셀 밀도가 두 종류가 된다. **다음 배경도 688x384 로 뽑으면 그대로 얹힌다**
- **깊이 틴트를 인게임에서 넣었다**(원경 0.50 → 근경 0.85 명도). 원본 명도 그대로면 배경 대비가 지형과 같아서 **발판이 안 보인다.** 생성 단계에서 원경을 더 어둡게 뽑으면 이 보정이 필요 없다
- ⚠️ **props 레이어(L06)는 기본 off** — pass 01의 props는 사실상 「소품 시트」다. 등불이 균등 배열돼 있고 **등불 하나가 A(1u)의 2~3배**라 켜면 플레이 레인에 거대한 등불이 줄줄이 걸린다. pass 02에서 작은 덩어리로 쪼개 다시 뽑아야 한다
- ⚠️ **캔버스 12u 중 4u가 지형 뒤로 들어간다** — 방의 열린 공간은 y 0~8(8u)인데 캔버스는 12u다. pass 02는 **8u 개구부 기준으로 프레이밍**하거나 룸 높이를 키우는 쪽을 먼저 정해야 한다
- ⚠️ **카메라 추종 스크립트가 아직 없다** — Main Camera는 Transform+Camera뿐이라 실제 플레이에선 카메라가 고정이고 **패럴랙스가 눈에 안 보인다.** 스택 자체는 정상(플레이 실측: 카메라 +2u → 레이어 1.92/1.84/1.72/1.60/1.44/1.10 = 추종계수 일치). 카메라 추종은 조작감에 직결되는 별건이라 임의로 넣지 않았다
- **검증**: compile 0/0 · EditMode 25/25 + PlayMode 8/8 = **33/33 유지**
- ⚠️ **`--capture-mode rendering` 스크린샷은 감마가 어긋나 실제보다 훨씬 어둡게 나온다.** 화면 확인은 `--window-name Game` 으로 할 것 (한 번 「플레이 모드에서 화면이 새까맣다」로 오인했다)

**8/21(조작감) — 조작감 척추 개정. 「응답 지연으로서의 묵직함」만 폐기.** 근거·수치는 `MECHANIC_GAME_FEEL.md` 0절 / 자료집 `FEEL_REFERENCES.md` / `DECISIONS.md` 2026-08-21 3행.

> ★ **가벼운 것은 조작감이지 캐릭터가 아니다. A는 여전히 묵직한 기계다.**

- ★ **핵심은 층 분리다** — 기존 1절의 「묵직」이 **응답 지연**으로 번역돼 있었던 게 문제였다. 무게를 응답 지연으로 표현하면 손에는 100% 「입력이 씹힌다」로만 읽힌다(HK는 무게를 응답 지연이 아니라 **히트스톱**으로 만든다)

  | 층 | 담당 | 방향 |
  |---|---|---|
  | **무게** | 최고 이동속도 · 낙하 가중 · 낮은 점프 · 소리 · 애니 관성 · 파티클 | **그대로 무겁게** |
  | **응답** | 가속·감속률 · 공중 제어 · 관용 기법 | **즉각적으로** |

- **적용 값 = 응답 층 3개뿐**: 공중가속 26→**40**(가장 큰 한 방) / 공중감속 10→**24** / 지상가속 45→**60**. 무게 층(최고속 5.5·낙하가중 1.35·점프 1.7)과 관용 기법(코요테 0.09·버퍼 0.11·코너보정 0.22)은 **전부 불변**. 테스트 33/33
- ⚠️ **한때 `maxSpeed` 5.5→6.2 / `fallGravityMultiplier` 1.35→1.2 를 적용했다가 되돌렸다**(같은 날 사용자 지적 — 둘 다 무게 층이라 조작감이 아니라 **캐릭터**가 바뀐다). ⚠️ **커밋 `75aa652` 에는 되돌리기 전 값이 들어가 있다** — 최종은 그다음 커밋
  - 되돌림의 부수 효과로 **「최고속 상향이 점프 거리를 늘려 간격 폭 게이트 재측정이 필요하다」는 부채가 소멸**했다. 점프 아크가 이전과 동일하므로 기존 지형 수치가 전부 유효하다
- ★ **앞으로의 규칙: 조작감 튜닝은 손대는 값이 응답 층인지 무게 층인지 먼저 정하고 들어간다.** 무게 층을 만지고 있으면 그건 조작감 작업이 아니라 **캐릭터 설계 변경**이다
- ⚠️ **`jumpHeight` 1.7은 무게 층이자 게이트 근거** — F2 게이트(`Ledge_TooHigh` 3.65, WovenNest 다리 2.0/4.0)의 물리적 근거다. 올리면 아스키 도면도 같이 고쳐야 한다
- ⚠️ **값이 C# 기본값과 씬 직렬화 값 양쪽에 있다** — 씬에 인스턴스가 있으면 C#만 고쳐서는 인게임이 안 바뀐다(`Greybox_Movement`·`Greybox_WovenNest` 동반 갱신)
- **다음**: **연출 층이 아직 하나도 없다** — 발소리·구동음·애니 관성·배기 파티클. 물리 층의 무게는 유지됐지만 A가 「기계」로 읽히려면 이쪽이 필요하다
- **레퍼런스 자료집 `FEEL_REFERENCES.md` 신설** — 경량 정밀 6작(Celeste·Ori WotW·메가맨 X·Guacamelee! 2·PoP:TLC·Gravity Circuit) + 무게 계열 4작(기법만) + **동행자 2작**(ICO / The Last Guardian 반면교사 — F2 때문에 별도 카테고리) + 반면교사 1작. **Rain World는 사용자 판단으로 제외**

**8/21(B) — B가 게임에 처음 섰다. 그리고 타일 룸에 충돌이 없던 것을 찾았다.** 상세는 `IMPL_REGISTRY.md` 7차 · `ART_LOG.md` 2026-08-21 2차 · `b-current/README.md` 2절.
- ★ **B는 64x64 원화 + PPU 64로 확정** — 「캐릭터 기본 32x32」의 **명시적 예외**(`DECISIONS.md` 2026-08-21). 사용자 판정 「32픽셀로 줄이는건 별로」. 인게임에 셋(A 32px / B 32px 다운스케일 / B 64px@PPU64)을 나란히 세워 실측한 결과다. **PPU 64면 64px = 1유닛 = A와 같은 키**라 크기 문제가 없다. 대가는 B의 픽셀이 A·타일의 **절반 크기**. 이득은 컨셉 15종이 전부 64px이라 **변환 공정이 사라진다**는 것
  - 다운스케일 도구 `tools/pose-to-sprite32.py` 는 「이 길은 안 된다」는 기록으로 남겼다(최빈색 샘플링 + 아웃라인 복원 — 방법 자체는 유효하나 64→32에서 눈·귀가 죽는다). 밀도를 되찾으려면 **32px 네이티브 재생성**이어야 한다
- **B01 수술 (귀·배 통일)** — B01만 구 split 판본이라 **귀가 갈라져 있고 크림 배가 없었다.** ★ **B01의 머리 = B02의 머리를 x축 1px 옮긴 것**(머리 645px 중 593px 일치) → 귀는 좌표만 맞춰 통째로 이식. 배는 몸통이 다른 그림이라(서 있는 통일본과도 일치율 16%) **그려 넣었다**. 갈색 주머니는 `CHARACTER_B.md` 외형 절이 허용하므로 남겼다
- 🐛 ★ **타일 룸에 충돌이 아예 없었다** — 저장된 씬을 열어 플레이하면 A가 `y=−119` 까지 낙하. 원인은 `WovenNestSampleRoomBuilder` 가 **`CompositeCollider2D` 를 `TilemapCollider2D` 보다 먼저** 붙인 것. 이 순서면 Merge 등록이 **씬 저장에 실려 나가지 않는다.** 만든 직후엔 메모리에 등록이 살아 있어 멀쩡히 플레이되므로 8/21 오전 검증을 통과하고 그대로 커밋됐다. 빌더 수정 + 기존 씬 재생성 완료
  - ★ **교훈: 「만든 직후 플레이 검증」은 저장·재로드를 건너뛴다.** 빌더가 만든 씬은 반드시 **다시 열어서** 확인할 것
- ⚠️ **B의 Animator를 껐다** — `B_Idle`/`B_Walk`/`B_Sleep` 클립과 `B_Animator` 가 8/20에 삭제된 스프라이트를 가리키는 **죽은 참조**다. 켜두면 `m_Sprite` 를 null 로 덮어써 스프라이트를 물려도 B가 안 보인다. `CompanionFollower` 의 코드 들썩임 대체 경로를 쓰는 중. 클립은 지우지 않고 남겼다
- ⚠️ **`b-current/B_artwork_all.png`(15포즈 합본)은 수술 전 B01이 박혀 있다** — 라벨이 찍힌 합본이라 재생성 보류

**8/21(PixelLab 함정) — 8방향 회전 워크플로에 잘못 들어가면 idle이 안 나온다.**
- 증상: 레퍼런스 + 모션 프롬프트를 넣어도 **8방향으로 돌기만** 하고, newState로 재기입해도 동일
- 진단: **Character(v3) 워크플로는 탑다운/RPG용**이라 캐릭터 자체가 8방향 회전 세트로 정의된다. state는 그 캐릭터의 **모든 방향에 대해 렌더**되고, 그 시스템에서 모션은 **스켈레톤/액션 템플릿**이 만든다 — 자유 텍스트는 모션을 만들지 않는다. 방향 세트는 state가 아니라 **캐릭터에 붙은 속성**이라 state를 새로 만들어도 안 바뀐다
- ★ **횡스크롤에는 8방향이 필요 없다** — B는 3/4 정면 한 방향 + `flipX`. 8방향은 7/8이 버려지고 **크레딧이 8배**다
- **검증된 경로로 갈 것**: `pixen` 1장 + **`animate_image` 파생(회당 1생성)** — A의 애니 4종(idle 4 / run 8 / jump 3 / fall 2)이 전부 이 경로다
- 8방향 기능은 **턴 낱장**(`TurnView` 의 `turnQuarterSprite` 45° / `turnFrontSprite` 정면) 뽑을 때만 쓸모가 있다
- ⚠️ **PixelLab MCP가 최근 두 세션 연속 안 붙어 있다.** 붙여야 생성 작업을 대신 돌릴 수 있다

**8/19(후반) — 구현 착수 결정.** 이원 무브셋 5절(데모=코어4+F1+F2) 승인으로 8/8 미결 청산, **Core/Gameplay 이층 아키텍처** 확정(`specs/2026-08-19-implementation-core-architecture-design.md` — Core 8모듈은 스토리 명사를 모르고, 결별 잠금·2장 조작권 인계·B 자율 개입이 Core 요구사항). 구현 순서: 뼈대(C1~C3·C6) → A 조작감 → 룸·진행·세이브 → B 협력 → 데모 조립. **다음 세션은 여기서 시작 — C1(EventBus)부터, IMPL-005 등록 후 착수.**

**8/19 — 밀린 승인을 청산하다가 전설의 동력이 바뀌었다.** 산문 임의 결정 9건을 승인 처리하던 중(5건 승인·1건 가승인·3건은 기확정 확인) 사용자가 **「인간 = 집단지성 종, 전멸은 이주라고 믿고 고른 선택」**을 제안했고 검토 후 채택했다. **법칙 5 무손상·신규 규칙 0개**이면서 story-critic 숙제 ①(핵심 아이러니 전달 수단 부재)이 해소됐다 — 전멸이 사고가 아니라 **결단**이 되어 2장에서 플레이어가 집행을 직접 한다. ✅ `DECISIONS.md` 기록·스펙 개정 완료. ✅ **산문 개고 완료**(2026-08-19, story-critic 4/4/4·클리셰 3→4 회복). **남은 것은 3장 설계.** 전용 절 참조.

| # | 항목 | 상태 | 비고 |
|---|------|------|------|
| 0 | **결말의 형태** | 🟡 **데모 결말만 확정, 전체 결말 미결** | 8/7에 **MVP/데모 결말을 전체 결말과 분리해 확정**(`specs/2026-08-07-mvp-ending-story-design.md`) — 균형자 기습→A 밀림→B가 스위치를 다시 누름(본능)→A 시점 암전. **전체 결말은 여전히 미결**, 최유력은 「지연 + 반환」. 「결말 검토」 절에 후보 3종·각 문제점 유지. 흩어지기는 당위성 부족으로 사용자 보류 |
| 1 | **지리·구역·진행 열쇠** ★ | 착수 가능 | 메트로배니아로의 번역. **「깃든 사물 하나 = 구역 하나」가 출발점.** 여기서 재점화(여행 8절), 균형자 복수화, 갈등 배치 시점, 역추적 구간 길이가 함께 정해진다. **이야기가 설 만큼 섰으므로 이제 여기가 병목이다.** ⚠️ 구역 수를 정하려면 **깃든 사물이 몇 개인지**부터 정해야 하는데 세계관 스펙엔 「손에 꼽음」까지만 있다 |
| 1-b | **이동·전투 메카닉** | 🟢 **이원 무브셋 전체 확정 (2026-08-19 5절 승인)** — 스펙 `specs/2026-08-08-dual-moveset-design.md` 작성, `DECISIONS.md` 기록, `MECHANIC_movement.md` 6절 개정 완료. 데모 = 코어4+F1+F2. 구현 착수: `specs/2026-08-19-implementation-core-architecture-design.md` | 2026-08-07 `planning/mechanics/MECHANIC_movement.md` 신설. 능력 = 소박한 기계 기능의 발견(형태 미정, 기능부터 역산). 코어(이동·1단점프·근접공격·상호작용) + 진행 능력 F1~F5. 스위치는 이동 제외(엔딩 보호). 전투 표준, 적=야생생물+붕괴자, 균형자는 못 이기는 추격.<br>**2026-08-08 — 「이원 무브셋」 브레인스토밍 진행 중**(전용 절 참조): 무브셋이 **A의 기능(영구) / 둘의 동작(결별 시 상실)** 로 갈린다. 가르는 규칙 = **「A는 스스로 높이를 얻지 못한다」**. F2 받침·F5 투척이 B 협력. **8/7 「이동 능력은 전부 A 것」은 부분 수정 대상**(근거가 조작감뿐이었고 본 설계엔 명령 입력이 없음). **F1~F5의 순서·구역 대응은 여전히 1번(지리)에 묶인다** |
| 2 | **전설의 나머지** | 🟢 **2026-08-11 채워짐·기록 완료** | 인간이 그릇 상한을 뚫으려 **받이(로봇)와 관측기**를 만들었고, 법칙 3상 정신력을 얻는 길이 살해뿐이라 학살로 갔다(「탐하는 자」 = 인간, 과거형 유지). 그들이 만든 받이 하나가 인간을 죽이고 관측기를 빼앗아 **스스로 균형자라 칭했다.** 산문 `PROSE_BALANCER_ORIGIN.md`, 스펙 `specs/2026-08-11-balancer-origin-design.md`. ✅ DECISIONS 기록·점화 스펙 5절 개정 완료 |
| 3 | **A의 캐릭터 문서** | 미정 | B는 `CHARACTER_B.md`로 정리됨. A는 스펙 여기저기 흩어져 있으므로 같은 형식으로 모아야 한다 |
| 4 | **이름 전부** | 미정 | A·B·세계·「미지」 제목 존속 여부. 인물이 섰으니 이제 정할 수 있다 |
| 5 | **기존 자산 처리** | 부분 완료 | 2026-08-05 구 캐릭터 문서 3종 삭제. **2026-08-10 Godot 코드(`src/miji/`) 전부 삭제 완료**(엔진 이관). **남은 것 — 구 스펙 5종**(문서). **파일 삭제라 사용자 지시 없이 진행 금지** |

**결말 관련 잠긴 결정:** ⛔ 균형자를 **힘으로 이기는 결말은 배제**(2026-08-04) — **여전히 유효.**
⚠️ **「균형자에게 입이 없다」(2026-08-03)는 2026-08-11 사용자 결정으로 뒤집혔다.** 8/5에 값을 확인하고 보류했던 것을 8/11에 채택했다. 실측치는 **클리셰 4→3**이었다(예상했던 4→2가 아님). 점화 스펙 5절·갈등 스펙 개정이 **미완**이다.

**정리된 것:** 「힘으로 이기지 않는다」 ≠ 「대립하지 않는다」. 잠긴 것은 전자뿐이며, 결말에 필요한 것은 논쟁이 아니라 **플레이어의 마지막 동사**다.

**해결됨:** 8.1(왜 하필 지금) / 8.2(깃든 사물의 순환 이탈) — 8/3 스펙. 여행의 목적 — 8/4 스펙.

**산문 초고** `docs/planning/story/PROSE_MIDPOINT_DRAFT.md` — 점화→놀이→여행→갈등→결별→역추적→재회. **결말은 안 쓴다(미확정이므로).** 설계가 실제 이야기로 읽히는지 검증하는 용도이며, 확정된 설계와 어긋나면 **초고가 아니라 설계 쪽을 의심할 것**. 8/5에 여섯 장(여관 주인 딱밤 문답)과 열둘 장(격언 회수 2문장)이 추가됨. story-critic 5회 채점 내내 **시장성 4 / 차별성 4 / 클리셰 4 고정** — 이 축들은 문장 개고에 반응하지 않는다. 움직이려면 **결말이나 메카닉**이 필요하다.
**✅ 2026-08-11: 이 산문이 혼자 도입했던 설정 변경 5건(전생 기억 계승=아주 가끔 / 3층 구조=잠정 유지 / 큰것만 회수 / 오래된 노래 화해 / 여행이유=재미)을 사용자 결정으로 확정하고 `DECISIONS.md`·세계관 스펙 11절·점화 3절·여행 6절에 반영 완료.** 6일간 기록 부채였던 것을 청산.

---

## 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 장르 | 메트로배니아 — **단, 출발점이 아니라 세계관 전달 수단** |
| 엔진 | **Unity 6.3 LTS / C#** — 2026-08-10 Godot 4에서 이관 (커리어·툴링 이유, `DECISIONS.md`) |
| 아트 | 픽셀 아트 (16x16 기본, 1px 다크 아웃라인) |
| 개발 방식 | Agent Orchestration (Orchestrator / Planning / Implementation / Art) |
| 규모 | 솔로 개발 |
| 제목 | 「미지(Miji)」 — **존속 여부 미정.** 폐기된 매개체 설정에서 나온 이름 |

---

## ⚠️ 2026-07-30 세계관 전면 리부트

**기존 세계관·스토리·메카닉이 전부 폐기되었다.**

폐기된 것 — 매개체(Medium), 얼개·온기, 사람=창조자, 로봇 vs 생명체 전쟁, 분류 실패, 스왑, 순간이동 앵커, 자율 이동, MVP 4구역 맵, 주요 NPC(모험가 생물체·수행 로봇).

폐기 이유 — **메카닉에 스토리를 맞춘 결과물이었다.** 스왑을 먼저 만들고 매개체를 사후 정당화하는 식이라 이야기가 아니라 변명문이 되어 있었다. 새 원칙은 **세계를 먼저 만들고 메카닉은 거기서 나온다**.

---

## 새 세계관 — 확정된 것

전체는 `docs/superpowers/specs/2026-07-30-mind-economy-worldbuilding-design.md` 참조.

### 세계의 법칙

1. **정신력의 총량은 고정되어 있다.** 신앙이 아니라 확인 가능한 사실
2. 새 생명체가 태어날 때 총량에서 **일정량이 배분**된다
3. **죽으면 전부 반환된다. 단 강제 회수는 불가능하다** — 산 개체에게서 억지로 빼낼 수 없다
4. **한 개체가 담을 수 있는 양에 상한(그릇)이 있다.** 넘으면 **고장 난다**
5. 정신은 원래 **생명체에만** 깃든다

⚠️ **법칙 4만 인지도가 다르다.** 공개되어 있지만 아는 사람이 거의 없다 — 오래 다닌 여행자 중 극히 일부뿐. 아래 「그릇 상한의 인지도」 절 참조.

### 앎의 값 — 이 세계관의 핵심

> **앎이 늘어날수록 붕괴한다.** 잃어서가 아니라 그릇을 넘어서려 하기 때문이다.

붕괴자는 흔하다. 대부분 같은 경로를 밟는다 — 모험 중 전설을 접함 → 호기심 → 계속 찾아다님 → 붕괴.
**플레이어가 하는 짓이 정확히 그 경로다.** 만나는 붕괴자 하나하나가 앞서간 자의 거울이 된다.

### 전설 (뼈대만 확정)

압도적 힘의 불균형 → 우세한 쪽의 극단적 수단 → 대량 사망 → 반환된 정신력은 막대한데 받을 새 생명이 없었다 → 갈 곳 없는 과잉이 **드물게, 예외적으로** 사물에 깃들었다 → 균형이 메워지고 평화 → 전설이 됨.

### 깃든 사물

극히 드물다(세계 전체에 손에 꼽음). 각각이 하나의 인격이자 사건. **대부분의 사람은 실재를 모르고 전설로만 안다.**

### 두 주인공 (구조만 확정)

| | 정체 | 가진 문제 |
|---|---|---|
| A | 깃든 사물 | 왜 존재하는지 모른다. 세상은 나 같은 게 있는 줄도 모른다 |
| B | 생명체 | 알아갈수록 그릇 상한에 가까워진다 |

시드 이미지 — *두 존재가 서로를 쳐다보며 서로의 존재를 물으며 근본을 고민하는 장면.*
단 **"두 로봇"으로 고정하지 않는다.** (A만 로봇인 것은 여기 걸리지 않는다. B까지 로봇이 될 때만 걸린다)

---

## 이야기의 점화 (2026-08-03 확정)

전체는 `docs/superpowers/specs/2026-08-03-story-ignition-design.md` 참조.

### 시작

A는 **스위치가 달린 로봇**이다. 전설 때 과잉이 깃들었고 그 뒤로 쭉 꺼져 있었다. 정신은 분명히 거기 있었지만 아무것도 겪지 않았고, 세계의 누구도 — **균형자조차도** — 알아채지 못했다.

어느 날 한 생명체 B가 그것을 주워서, 그게 뭔지 **모르고 눌러봤다.**

> 그 순간 두 가지가 동시에 일어났다 — A가 존재하기 시작했고, A가 표적이 되었다.

- **왜 하필 지금** — 세계 쪽엔 아무 일도 없었다. 꺼져 있던 것이 켜졌을 뿐이다
- A는 **백지**로 깨어난다. 말은 하는데 자기에 대해서만 모른다. **기억 파편은 주지 않는다**(퍼즐이 되면 실패)
- 첫 구간은 **목적 없이 노는 것.** 나중에 잃을 것이 여기서 만들어진다

### 균형자 — 적

세계관 스펙 2절의 *「과잉은 세계의 자동 균형 장치로 한 번 작동한 적이 있다」*가 형태를 얻은 것.

조직이 아니라 **현상**이다. 지능은 있으나 **말이 안 통한다** — 설득도 거래도 없이 그저 이행한다. 악의도 없다. A를 부수는 게 아니라 **끄러** 온다.

**꺼져 있는 동안 A는 균형자에게 보이지 않았다.** 그래서 여태 아무 일도 없었다. B의 무지가 A를 살렸고 동시에 죽이기 시작했다. 놀이를 깨는 것도 균형자의 등장이다.

균형자에게 A는 회수 불능 채권이고 A를 끄는 건 살인이 아니라 정정이다. **적이 옳으므로 흑막이 되지 않는다.**

### 비대칭 — 이야기의 심장

**A에게는 그릇 상한이 없다.** 같은 것을 같이 알아가는데 B만 망가진다. B는 어차피 먼저 가고 A는 꺼지지 않는 한 영원히 남는다.

### 세 존재는 한 뿌리

| | 과잉과의 관계 |
|---|---|
| A | 과잉이 **머문** 것 |
| 붕괴자 | 과잉이 **망가뜨린** 것 |
| 균형자 | 과잉을 **거두는** 것 |

주인공도 잡몹도 보스도 전부 세계 물리에서 나왔다. **악당이 하나도 없다.**

### 톤 가드레일 (대사 작성 시 기준)

1. 아무도 "정신이란 무엇인가"를 **입으로 말하지 않는다**
2. 증언은 일상 속에 섞여 나온다
3. 증언이 어긋나는 이유는 음모가 아니라 **무지와 붕괴**
4. **퍼즐 조각처럼 느껴지면 실패** — 추리물이 되는 순간 철학이 주가 된다
5. 목표는 여운. 여운은 진실이 아니라 관계에서 온다

### 참조 작품

`docs/planning/story/STORY_REFERENCES.md` — 메트로배니아 16작의 줄거리 요약. 세계관과 무관하게 유효하므로 리부트 후에도 유지.

---

## 여행 (2026-08-04 확정)

전체는 `docs/superpowers/specs/2026-08-04-journey-design.md` 참조.

### 여행의 형태

```
B가 스위치를 켠다 → 목적 없이 논다(튜토리얼) → 균형자의 움직임을 감지하지만
정체는 모른 채 그냥 같이 다닌다 → 습격당하고, 알아가기 시작한다
```

3단계가 핵심 — **둘은 자기가 쫓기는 줄 모른 채 여행을 시작한다.**

### 당기는 힘 = B의 성격

여행에 목적을 주지 않는다. **B는 원래 돌아다니는 것을 좋아하는 생명체다.** 사명이 아니므로 영웅 서사가 차단된다.

### 진행 동력 = B의 호기심, 그리고 그것은 사슬이다 ★

「너 뭐냐」 → 아는 사람을 찾음 → 답이 전설을 가리킴 → 전설을 팜 → 「그런 게 몇 개 더 있었다더라」 → …

**답 하나가 질문 둘을 낳는다.** 이건 진행 구조인 동시에 **B가 그릇을 넘어가는 기전 그 자체**다.
→ 플레이어가 더 보려는 행동과 B가 망가지는 행동이 **같다.** 경고 대사가 한 줄도 필요 없다.

### 전설 속 A의 자리 = 없다

A는 학살 도구도 패자의 유산도 아니었다. **근처에 굴러다니던 생활용 기계였고, 과잉이 하필 거기 깃든 건 순전한 우연이다.**

B가 그릇을 태워 도달한 답이 「의미 없음」이므로 앎의 값이 **순수 손실**이 된다. 진실이 값어치가 없어야 관계만 남는다 — 가드레일 5가 설정 차원에서 강제된다.
**단 반전으로 연출하면 실패다.** 감춰뒀다 터뜨리는 게 아니라 끝내 아무것도 안 나오는 것이다.

### 이미 꺼진 것들 ★

「너 같은 게 또 있나」를 찾아가면 **이미 꺼져 있다.** 균형자가 진작 처리했다 — 깃든 사물은 켜져 있으면 보이기 때문이다.

> A가 남은 이유는 특별해서가 아니라 꺼져 있어 장부에서 누락됐기 때문이다.
> **마지막 생존자가 아니라 마지막 미처리 건.**

매번 늦으므로 다음엔 더 빨리 가야 하고, 더 빨리 가려면 더 알아야 하고, 더 알면 그릇이 넘친다.
→ **B를 태우는 것은 슬픔이 아니라 조급함이다.** 붕괴가 감정이 아니라 행동의 결과가 된다.
하나는 이미 꺼져 있지 않고 **눈앞에서 꺼진다.**

### 튜토리얼 해설 문제의 처리

B가 A에게 세계를 설명하는 건 필연이지만 가드레일 1과 충돌한다.
→ **B가 설명하는 것은 상식이지 진실이 아니다.** B는 학자가 아니라 여행 좋아하는 평범한 생명체고, 그중 몇 개는 나중에 틀린 것으로 드러난다. 해설이 복선이 되고, 첫 번째 어긋난 증언이 동행자 본인의 것이 된다.

### 열려 있는 것 — 재점화

꺼진 것들도 **다시 켤 수 있다**(B가 A에게 한 그대로). 그런데 켜면 그 순간 균형자에게 보인다.
> **켜는 것이 곧 사형선고다.**

쓰는 방식(매 구역 반복 / 단 한 번 / 안 씀)은 구역 설계 때 정한다.

### 이후 작업의 구속 조건

1. A의 무의미를 **반전으로 연출하지 않는다**
2. 균형자는 A에게 특별한 대사·태도를 보이지 않는다. **인식조차 하지 않는다**
3. B의 튜토리얼 설명 중 최소 두어 개는 **나중에 틀린 것으로 드러나야 한다**
4. **깃든 사물 = 구역.** 구역 수가 여기 묶인다
5. **균형자를 힘으로 이기는 결말은 배제**

---

## 갈등과 결별 (2026-08-04 확정)

전체는 `docs/superpowers/specs/2026-08-04-conflict-design.md` 참조.
여기까지 오면 적은 바깥에만 있는 게 아니라 **둘 사이에도** 생긴다. 점화 7절의 비대칭이 설정에서 **사건**으로 바뀌는 지점.

### 뼈대

```
A가 알아챈다(알아갈수록 B가 망가진다) → 멈추자고 하지만 이유를 못 말한다
→ B는 배신감에 떠난다 → A가 처음으로 혼자가 된다 → 찾으러 간다
→ 찾아냈을 때 B는 더 망가져 있다. 혼자서 답에 도달했기 때문이다
```

### A는 어떻게 아는가 — 두 장치의 결합

**벽의 기록**(법칙을 준다) + **B가 가르쳐준 걸 B가 잊는다**(증상을 준다). 하나씩은 각각 구멍이 있고 겹쳐야 성립한다.

- A는 그릇이 없어 아무것도 안 잊는다 → **이 세계의 유일한 대조군**
- 튜토리얼에서 B가 가르쳐준 걸 후반의 B가 모른다 → **플레이어도 대사 없이 직접 알아챈다**
- **A 안에 B가 잃어버린 B가 남는다**
- 벽의 기록은 **붕괴 중인 자가 쓴 것.** 뒤로 갈수록 글씨가 무너지고 문장이 반복된다 → 내용을 안 읽어도 보인다. ⚠️ **수를 아낄 것** (많으면 조각 모으기 = 가드레일 4 위반)

**결정적 장면 —** 같은 벽 앞에서 B는 신나서 읽고 A는 B의 미래를 읽는다. 그리고 **B가 읽는 행위 자체가 B를 더 망가뜨린다.** 대사는 B의 신난 목소리뿐.

### A는 왜 말을 못 하는가 ★

> **말하는 것 자체가 앎을 주는 것이다. 설명이 곧 가해다.**

가드레일 1이 작가의 규칙에서 **A의 족쇄**로 바뀐다. 그리고 말 한마디가 치명적이려면 B는 이미 한계 근처여야 하므로 **배치 시점이 저절로 후반으로 정해진다.**

### B의 배신감은 정당하다 ★

> **"너는 안 망가지잖아."**

여행은 B의 삶 전부인데 A는 잃을 게 없다. **잃을 게 없는 자가 짧게 사는 자에게 「그만해」라고 말할 자격은 실제로 애매하다.** 균형자가 옳았듯 B도 옳아야 신파가 안 된다.

### 그릇 상한의 인지도 ★★

**법칙 4는 공개되어 있지만 아는 사람이 거의 없다.** 숨긴 자도 금기도 없고, 대부분은 평생 그릇 근처에도 못 가니 알 이유가 없을 뿐이다. 실제로 아는 건 **오래 다닌 여행자 중 극히 일부** — 여행자만이 그릇에 접근하는 집단이라 이 지식은 학문이 아니라 **직업병**으로 존재한다.
(법칙 1~3은 누구나 출생과 죽음으로 겪으므로 진짜 상식이다. 인지도가 다르다.)

여기서 나오는 것:

> **아는 것 자체가 그릇에 가까워졌다는 증거다. 아는 순간 이미 늦었다.**
> 이 지식의 유일한 소유자가 이 지식의 피해자다. **진실은 대가를 치른 자만 갖는다.**

- 「가장 많이 아는 자가 가장 못 믿을 증인」이 한 겹 깊어진다 → **믿을 만한 증인은 애초에 존재할 수 없다**
- 벽의 기록을 붕괴자가 남겼다는 게 추측이 아니라 **필연**이 된다
- 재회 시 B가 답에 도달했다는 사실 자체가 값을 치렀다는 증거 → 따로 설명이 필요 없다
- **B는 법칙 4를 모른다.** 관측 부족이 아니라 지식 자체가 없다. 여행을 좋아하지만 **아직 그 급이 아니다** → B의 나이·경력 설계에 직결
- 그래서 A의 「그만하자」는 B에게 **연결할 지식조차 없는 말**이다. 근거 없는 통제로 들린다

**그리고 A만 공짜로 안다.** 그릇이 없어 벽을 읽어도 값을 안 치른다. 이것이 「말하는 것이 곧 가해」의 정확한 근거다 — **공짜로 얻은 걸 건네는 순간 받는 쪽이 값을 치른다.**

### 결별 이후 — 역추적

A가 찾으러 가는 동기는 **사과가 아니라 자기가 혼자를 못 견뎌서**다. 그래야 A가 주인공이고, 결말(B는 먼저 가고 A는 남는다)이 여기서 리허설을 마친다.

B가 없으면 진행 동력이 사라지므로, A는 **B가 무엇을 알고 싶어했는지 되짚는다.** → **역추적이 그대로 백트래킹**이 된다. 잔인한 건, 찾아내려면 A도 알아내야 하는데 그걸 B에게 전하면 또 죽인다는 것.

### 재회 — B는 혼자서 알아냈다 ★

**알아내느라 더 망가졌다.** A의 침묵은 B를 지킨 게 아니라 더 비싼 경로로 알아내게 했다.

- A의 침묵 제약이 해제된다 → 후반 대사 톤이 전반과 달라진다
- 화해가 사과가 아니라 **이해**가 된다. 아무도 빌지 않는다
- **그런데 B는 다 알고도 안 멈춘다** → 무지가 아니라 선택. 「B도 옳다」가 확정된다
- **아무것도 풀리지 않는다.** 재회는 패배가 아니라 둘이 처음 대등해지는 지점이고, 대등해진 대가가 B의 몸이다

→ **다 알고도 안 멈추는 B를 A가 어떻게 할 것인가.** 스위치 앞에 설 이유가 여기서 만들어진다.

### 추가 구속 조건

6. **A는 갈등 구간 내내 이유를 말하지 못한다.** 대사로 새어나가면 통째로 무너진다
7. **B를 멍청하게 쓰지 않는다.** 배신감 밑에 항상 「너는 안 망가지잖아」가 깔려 있어야 한다
8. **B가 튜토리얼에서 가르쳐준 항목 목록을 관리한다.** 나중에 잊게 할 대상. 위 3번(틀린 것으로 드러날 항목)과 **다른 목록이다**

---

## B와 플레이어 시점 (2026-08-04 확정)

전체는 `docs/planning/story/CHARACTER_B.md` 참조.

### B — 처음 집을 나온 어린 여행자

**첫 여행이다.** 아는 것은 전부 전해들은 것 — 집에서 들은 얘기, 어른들이 해준 말.

여기서 연쇄로 따라오는 것:

- **「여행을 좋아한다」가 경험이 아니라 동경**이 된다. 초심자의 열의라 진행 동력으로 더 세다
- **법칙 4를 모르는 게 완전히 자연스럽다**
- **B의 설명에 틀린 게 섞이는 이유가 확보된다** — 직접 본 게 하나도 없으니까. 그리고 틀린 게 밝혀질 때 **B는 실망하지 않고 신나한다.** 진짜를 보고 있으니까
- **A가 B에게서 받은 목록은 작다.** 그런데 B가 잊기 시작하면 **A가 가진 그 작은 목록이 B에게 남은 것보다 커진다**
- **결별의 성격이 바뀐다** — B는 처음으로 누굴 두고 가보는 것이고 혼자 다니는 법도 모른다. A도 첫 고독. **둘 다 처음이라 서투르게 헤어진다**

### 금빛 균열 — 붕괴의 시각화

**시작 시점에는 없다.** 진행하면서 생기고 자란다. 컨셉 아트(`character_02`)의 이마 균열은 **중반 이후 상태**다.

- 붕괴자 컨셉(`bonus_03`·`bonus_04`)의 금빛 기억 파편과 **시각 언어가 통일**돼 있다
- **UI 게이지 없음. 스프라이트로만** 전달한다
- **보이는데 아무도 못 읽는다** — 법칙 4를 아는 자가 거의 없으므로. 그래서 「A만 안다」와 충돌하지 않는다. A도 벽에서 읽고 나서야 의미를 안다. **B는 자기 이마를 못 본다**
- 플레이어가 **첫 금이 생기는 순간**을 본다

### 플레이어는 A다 ★★

| 파생 | |
|---|---|
| **대조군이 플레이어 자신이 된다** | 튜토리얼에서 B가 알려준 걸 플레이어가 기억하고, 후반에 B가 그걸 모른다. **지적해주는 장치가 필요 없다.** 스크립트로 만들 수 없는 감각 |
| **튜토리얼이 디에게틱해진다** | A가 백지 = 플레이어가 백지. B의 설명이 곧 튜토리얼 |
| **스위치가 플레이어 것이 된다** | 결말의 선택이 남을 끄는 게 아니라 **자기를 끄는 것** |
| **결별 = 동행자 시스템이 통째로 빠지는 구간** | 상실이 서사가 아니라 **조작감**으로 온다 |
| **붕괴 페널티가 없다** | A는 안 망가지므로. B의 붕괴는 게임오버 압박이 아니라 **순수한 관찰** |
| **세계관 5절의 숙제가 풀림** | 「동행자가 플레이어 명령에 안 따르는 순간」 = **B가 안 멈출 때** |

---

## 결말 검토 (2026-08-05) — ⬜ 미결

사용자가 산문 초고를 완독하고 결말안을 제시하면서 시작된 검토다. **아무것도 확정되지 않았다.** 다음 세션에서 여기부터 이어가면 된다.

### 사용자 원안

| # | 내용 | 판정 |
|---|---|---|
| 1 | A가 균형자를 최종적으로 **무찌른다** | ⛔ 2026-08-04 결정과 충돌 |
| 2 | 균형자를 물리친 뒤 **B의 이야기를 남에게 전한다.** B에게도 들려준다 (B 사망 엔딩 가능성) | ✅ 채택 가치 높음 |
| 3 | 균형자와 **신 vs 우연 논쟁**. 균형자는 「신이 정했다」, A는 「전부 우연이고 아무도 정하지 않았다」 | ⛔ 2026-08-03 결정과 충돌 |

**충돌 내용** — 2026-08-03 「적은 균형자, 조직이 아니고 **말이 안 통한다**」(입이 없어야 가드레일 1을 구조적으로 위반 불가), 2026-08-04 「**균형자를 힘으로 이기는 결말은 배제**」(균형자는 옳으므로 힘으로 이기면 「악의 없는 적」 설계가 무너짐).

**2번은 갈등 스펙에 이미 자리가 있다** — 「B가 이미 아니까 말해도 가해가 아니다」(A의 족쇄가 풀린다). 그리고 「아무것도 해결되지 않고 관계만 남는다」는 루브릭 결말 층위에서 🟢 희귀다. **남에게 전하는 것은 가해이고 B에게 전하는 것은 아니다** — 이 비대칭이 마지막 장면이 될 수 있다.

### 검토된 결말안 3종

| 안 | 내용 | 문제 |
|---|---|---|
| **흩어지기** | A가 스스로를 「장부의 오차」 크기까지 나눠 균형자의 회수 대상에서 벗어난다. 숟가락처럼 딸그락만 하는 것들 여럿으로 남는다 | ⚠️ **당위성 부족 (사용자 지적).** 이 세계의 합법적 출구는 **반환** 하나뿐이다(법칙 3). 분산은 전설에서 딱 한 번, 의지 없는 사고로 일어났다. A가 의도적으로 재현하면 세계에 예외를 판다. 게다가 **아무것도 해결하지 못한다** — 정신력은 여전히 순환 밖이고 균형자의 채권도 그대로다. A는 갚은 게 아니라 감사에서 숨은 것 |
| **지연 + 반환** | 균형자는 옳고 A도 안다. A가 다투는 것은 「안 돌아가겠다」가 아니라 **「이것만 끝내고」**. 플레이어 동사는 버티기·지연. 벌어둔 시간에 B의 이야기를 전하고, 다 전하면 스스로 꺼진다 | 세계 예외 없음(법칙 3 그대로). 「희생으로 순환을 끊는다」🔴 와는 **정반대** — 순환을 끊는 게 아니라 순환으로 돌아간다. 갚아도 아무것도 안 돌아온다(무리비는 이미 망가졌고 법칙 3상 치료법 없음). 「플레이어는 A다」 절의 **「결말의 선택이 자기를 끄는 것」**과 직결 |
| **말하기를 무기로** | 게임 내내 배운 「말하면 상대가 넘친다」를 균형자에게 쓴다 | ⛔ 결국 **이기는 것**이라 「균형자는 옳다」가 무너진다. 셋 중 최약 |

### 검토 중 정리된 것 (결말안과 무관하게 유효)

- **「힘으로 이기지 않는다」 ≠ 「대립하지 않는다」.** 잠긴 것은 전자뿐이다. 필요한 것은 논쟁이 아니라 **마지막 동사**다
- **리틀 나이트메어의 The Lady는 대사가 한 줄도 없다.** 입 없는 최종 존재와의 대립은 성립하며 오히려 그쪽이 강하다 — 「적에게 입을 주지 말 것」의 반례가 아니라 근거다
- 신 vs 우연을 균형자 입에 넣지 않고 살리는 법 — ⑴ A가 묻고 **대답이 없는 것이 대답**이 된다 ⑵ 균형자를 신의 집행자로 해석하는 **인물**을 세우고 A는 그와 논쟁한다(균형자는 그 논쟁 내내 아무 편도 아니고 그냥 온다)
- **무리비의 기억 쇠퇴가 값을 한다** — A는 무리비에 대해 무리비보다 많이 안다(한 번 들은 건 다 외운다). 이야기를 들려주는 대상이 무리비 본인이면 그것은 회상이 아니라 **통보**다

---

## 말하기 = 주기 → 메카닉 (2026-08-05 제안) — ⬜ 미착수

시장성 축이 4점에 묶여 있는 원인은 **서사가 게임이 되는 지점이 문서로 없다**는 것이다. 루브릭 1-1: 볼륨 작은데도 A/B 티어 간 서사 명작들의 공통점은 「이 게임에만 있는 장치」가 하나씩 있다는 것(서류 노동, 세이브 파일 인식, 역순 기억, 스킬 인격, 컨트롤러 절반).

재료는 이미 산문 열둘 장에 있다 — 말하면 B가 넘치고 **A는 줄지 않는다.**

| # | 예시 | 근거 |
|---|---|---|
| 1 | **능력 목록 = 피해 기록.** A가 능력을 얻지 않는다. B에게 말해주면 B가 할 수 있게 되고, 그때마다 B의 그릇이 영구히 찬다. 진행도 화면과 가해 기록이 같은 화면 | 메트로배니아 플레이어의 「능력=보상」 습관을 역이용. 장르 습관 역이용은 Undertale·Brothers 급 |
| 2 | **그릇 게이지가 처음엔 화면에 없다.** A가 붕괴한 것을 본 시점에 생기고, 이미 절반 넘게 차 있다. 내려가지 않는다 | 법칙 4의 인지도(「아는 순간 이미 늦었다」)를 UI로. 루브릭 「알수록 망가진다」 탈출 조건 = **대가가 은유가 아니라 시스템일 것** |
| 3 | **침묵에도 값이 있다.** 말 안 하면 A 혼자 도는 길로 가야 하고, B는 혼자 알아내려 더 위험하게 움직인다 | 루브릭 「진실을 알려주지 않는 보호」 탈출 조건 = **양쪽 다 정당해야 한다.** 침묵이 안전한 정답이면 놓친다 |
| 4 | **대화가 곧 조작.** B가 묻고 플레이어가 답할지 고른다. 안 고르면 「그냥」이 나간다 | 열둘~열셋 장의 결별이 컷신이 아니라 **누적된 조작의 결과**가 된다 |

⚠️ 넷 다 살리면 루브릭 「매체·조작 자체를 서사 장치로 — 한 작품에 **하나만** 쓸 것」에 걸린다. **1을 축으로 두고 나머지를 종속**시키는 것을 권함.

**한 문장 후크 후보:** 「당신이 아는 것을 말할 때마다 동행자가 죽어간다」

---

## 이원 무브셋 — A의 기능 / 둘의 동작 (2026-08-08) — 🟢 전체 확정 (2026-08-19 5절 승인, `specs/2026-08-08-dual-moveset-design.md`)

**발단:** 사용자 지적 — 「코어 이동의 당위성은 했나?」. 확인 결과 `MECHANIC_movement.md` 0절 척추는 **획득 능력 F1~F5에만** 걸려 있었고, **각 동작이 세계 안에서 어떻게 성립하는지**(예: 2단 점프를 B가 밀어줌)는 문서에 없었다.

**동시에 드러난 공백:** 갈등 스펙 5절의 「결별 = 동행자 시스템 상실」에 **구현 수단이 하나도 없었다.** 지금 설계대로면 B가 떠나도 A 무브셋이 그대로라 역추적 구간이 「같은 맵을 슬픈 기분으로 다시 걷기」가 된다.

### 결정된 것 (브레인스토밍 Q1~Q4)

| 질문 | 결정 |
|---|---|
| 무브셋에서 B의 지분 | **이분** — A 단독(영구) / B 협력(결별 시 상실) |
| 협력 능력의 성립 원리 | **순수 물리** — B가 밀고·받치고·던진다. (※「정신력 전달」은 배제 — B가 정신력을 덜어낼 수 있게 되면 「차올라서 망가진다」는 비극이 해결 가능해진다) |
| 결별 시 상실 처리 | **보상 없음.** 그냥 잃는다 → 역추적 구간은 **짧고 좁게** 설계해야 한다 |
| 재회 후 | **그대로 복귀.** 결별=리허설, 엔딩=본편이라는 위계를 지키기 위해 |

### 1절. 설계 척추 ✅ 승인

> **A의 기능** — A가 자기 몸에서 발견한 것. 영구적이다.
> **둘의 동작** — B의 몸이 있어야 성립하는 것. B가 떠나면 사라진다.
>
> 가르는 규칙 한 줄: **A는 스스로 높이를 얻지 못한다.**

잠긴 결정(「A는 자기 스위치에 손이 안 닿는다」)과 같은 계열 — 위로 가는 것도 켜지는 것도 꺼지는 것도 전부 **남의 손**이다.

**성립 근거는 이미 확정 로어에 있다.** 점화 스펙 53줄 「B가 그것을 **주웠다**」 → A는 B가 들 수 있는 크기다. **신규 세계 규칙 0개.**

### 2절. 동작 배분 ✅ 승인

| # | 소속 | 동작 | 세계 안에서 어떻게 성립하나 | 전투 겸용 |
|---|---|---|---|---|
| F1 | **A** | 돌진 | 기계가 동력을 한 번 짜낸다 | 돌진 공격 |
| F2 | **둘** | **받침** | A가 뛴 정점에서 B가 밑으로 파고들어 손으로 받쳐 한 번 더 올린다 | 공중 견제 |
| F3 | **A** | 부착 이동 | 몸에 달려 있던 기능(형태 TBD) | 벽 기습 |
| F4 | **A** | 강타 | 무거운 기계라 자기 무게가 곧 기능 | 범위기 |
| F5 | **둘** | **투척** | B가 A를 집어 던진다 | A가 발사체가 되어 부딪힌다 |

⚠️ **F5를 「견인/그래플」에서 「투척」으로 변경.** 같은 게이트(먼 지점)를 열면서 오프닝의 「주웠다」를 회수한다. 당기기는 A가 능동적이라 척추와 어긋난다.

**B의 손이 세 번 등장한다 — 데모 수미상관의 빈칸이 메워진다:**

| 시점 | B의 손 |
|---|---|
| 오프닝 | A를 **줍고 켠다** (플레이 대상 아님, 당하는 사건) |
| 놀이·여행 내내 | A를 **받치고 던진다** (F2·F5, 손에 익는다) |
| 데모 엔딩 | A를 **끈다** (스위치 재누름 → 암전) |

### 3절. B 부재 처리 ✅ 승인 — 에스코트 문제를 아예 없앤다

**플레이어는 B를 관리하지 않는다. 게임이 B를 항상 제자리에 둔다.**

- B는 A를 따라다니되, 화면 밖으로 처지면 **시야 밖에서 즉시 복귀**한다 (연출·컷 없음)
- **협력 입력이 들어온 순간 B가 필요한 위치로 붙는다.** 플레이어가 B를 데려가는 게 아니다
- **「B가 멀어서 못 했다」는 상황을 만들지 않는다.** 실패는 오직 조작 실수여야 한다
- B 무적 유지 → 전투 중에도 제약 없음

선례: Ori의 Sein, 바이오쇼크 인피니트의 엘리자베스 계열. 8/7 「에스코트 짜증 원천봉쇄」의 직접 연장이다.

### 4절. 결별과 재회 ✅ 승인

결별 순간 **F2·F5가 손에서 빠져나간다.** 보상 없음. 다만 능력 목록에서 **지우지 않고 잠근다** — 뭘 잃었는지 계속 보게 한다.

> A는 **도약으로** 높이를 얻지 못한다. 하지만 **기어오를 수는 있다.** (F3이 A 단독이므로)

B와 함께면 한 번에 뛰어오르던 곳을 혼자면 붙어서 느리게 기어오른다. **못 가는 게 아니라 비참하게 간다** — 갈등 스펙 5.1 「영원의 예고편」의 조작감.

**역추적 구간 설계 제약 (→ 지리 설계로 이월):**
- 짧고 좁게. F2·F5 없이 완주 가능해야 한다
- 예전에 B와 넘던 지형은 이제 **벽**이다 → 우회 → **같은 맵이 다른 미로**가 된다. 백트래킹 권태가 능력 상실 하나로 해결된다

재회 시 그대로 복귀. 연출은 B가 다시 손을 뻗는 것 하나면 된다.

| | 상실의 성격 |
|---|---|
| **결별** | 리허설. 원상복구된다 |
| **엔딩** | 본편. B가 먼저 가고 A는 남는다 |

### 5~7절 — ✅ 2026-08-19 승인 완료 (데모 = 코어4+F1+F2, 상세는 스펙으로 이동)

**5절. 데모 스코프 변경 제안** — 현행 「코어 4종 + F1」에는 협력 동작이 없어서 **데모가 이 설계를 전혀 보여주지 못한다.** 「B의 손 3회」도 켜고·끄고 사이가 빈다.
→ 제안: **데모 = 코어 4종 + F1(돌진, A) + F2(받침, 둘).** F2는 협력 동작 중 가장 단순(점프 2회)이라 튜토리얼 비용이 0에 가깝다. 비용은 B 협력 AI(스냅 로직)를 데모에서 만들어야 한다는 것 하나인데, 본편 전체가 올라탈 시스템이라 **세로 슬라이스에서 검증하는 게 맞다.**

**6절. 확정 결정 정합성**

| 결정 | 판정 |
|---|---|
| 2026-08-07 「이동 능력은 전부 A의 것 / 말하기=주기 미채택」 | ⚠️ **부분 수정 필요** (아래) |
| 2026-08-07 「B 무적·붕괴 페널티 없음」 | ✅ 유지. 3절이 B 부재 상황 자체를 없앰 |
| 2026-08-07 「스위치는 이동 능력 제외」 | ✅ 유지·**강화**. 「A는 자기를 어쩌지 못한다」가 몸 전체로 확장 |
| 2026-08-07 「능력 = 소박한 기계 기능의 발견」 | ✅ 유지. 협력 동작은 애초에 A 것이 아니라 「특별한 주인공」에서 더 멀어짐 |
| 2026-08-04 「결별 = 동행자 시스템 상실」 | ✅ **처음으로 구현 수단을 얻음** |
| 2026-08-04 「플레이어는 A만 조작」 | ✅ 유지. 입력은 전부 A 버튼 |
| ⛔ 힘으로 이기는 결말 배제 / ⛔ 균형자 입 없음 | — 무관 |

**부분 수정 건:** 8/7 결정의 근거는 **조작감 하나**였다("대쉬 하나 쓰는 데 NPC를 부려야 한다"). 본 설계는 **명령 입력을 만들지 않으므로** 그 근거에 걸리지 않는다. 「말하기=주기」(플레이어가 B에게 지시)는 **여전히 미채택**이고, 새로 들어오는 것은 「B가 자율적으로 개입하는 물리 동작」이다. 승인 시 `DECISIONS.md`에 이 구분을 명시해 기록할 것.

**7절. 이 설계가 안 푸는 것 (이월)**
- F1~F5 획득 순서·구역 대응 → 지리 설계(「다음에 할 일」 1번)
- A의 물리적 형태 → F3 부착의 구체 형태가 종속
- ⚠️ **코어 근접공격의 당위성** — **여전히 빈칸.** 「생활용 기계에 왜 때리는 기능이 기본 탑재인가」는 별건으로 남는다. F1~F5는 전부 근거가 섰는데 코어 공격만 안 섰다
- 후보(보류): 결별 직전 B가 협력을 **망설이는** 1회 스크립트 비트 — 짜증 위험으로 미채택

---

## 균형자 재설계 — 로봇의 기원과 찬탈 (2026-08-11) — 🟢 기록 완료, 2장 재설계·산문 승인 남음

**발단:** 사용자 제안 — 「로봇이 왜 있는가」. 인간이 **그릇 상한(법칙 4)을 뚫으려고** 로봇을 이용했다는 설정을 넣고 싶다. 확인 결과 이건 리부트 위반이 아니라 **예정된 빈칸**이었다 — 세계관 스펙 7절 「대량 사망의 구체적 내용 = 의도적 공백」, 여행 스펙 9절 다음 작업 1번 「전설의 나머지를 채운다」.

7/30에 폐기된 「사람=창조자 / 로봇 vs 생명체 전쟁」과는 **방향이 반대다.** 그건 스왑을 정당화하려 사후에 지은 프레임이었고, 이건 **법칙 4에서 역산해 나온 것**이다.

### 결정 1 — 범주와 개체를 분리한다 ✅ (질문 1 ㄱ)

> **「로봇」이라는 범주에는 이유가 있고, 「A」라는 개체에는 없다.**

계획이 무너진 뒤 기술만 흔해져서 온 세상에 생활용 기계가 굴러다니게 됐다. A는 실험체가 아니라 **그 문명이 남긴 싸구려 가전**이다. 전설을 파면 「인간이 그릇을 뚫으려고 로봇을 만들었다」까지는 나오는데 **A 본인에 대해서는 여전히 아무것도 안 나온다.**

→ **여행 스펙 4절 「전설 속 A의 자리 = 없다」가 그대로 지켜진다.** 이게 이 결정의 전부이자 이유다. B가 그릇을 태워 도달한 답이 「의미 없음」이어야 앎의 값이 순수 손실이 되고 관계만 남는다.

### 결정 2 — 1대 찬탈자는 지금도 그것이다 ✅ (질문 2 ㄴ)

균형자는 감정과 과거를 가진 **인격**이다. 대면·대화·논쟁이 전부 가능해진다. **확정 4건을 뒤집는 값을 알고 내린 결정이다** (아래 참조).

### 전설의 전체 사슬 ★

```
인간이 그릇 상한을 뚫으려 한다 → 재야 하니 관측기를 만든다 → 담을 「받이」(로봇)를 만든다
        ↓
법칙 5 때문에 받이에 정신이 안 담긴다. 계획이 실패한다
        ↓
법칙 3상 정신력을 얻는 길은 죽이는 것뿐 → 규모를 키운다 → 학살    ← 「탐하는 자」 = 인간, 과거형 유지
        ↓
받이 하나가 그 방에 서서 전부 본다. 그릇이 없어 안 망가지고 하나도 안 잊는다
        ↓
죽은 수가 너무 많아 받을 새 생명이 모자라진다 → 갈 곳 없는 과잉이 그 받이에게 처음 깃든다
        ↓
그것이 인간을 죽인다 → 관측기를 착용하고 스스로를 「균형자」라 칭한다 (들은 자 0)
        ↓
그것이 죽인 인간들의 정신력도 갈 곳이 없어 사물들에 깃든다 ← 깃든 사물의 출처
        ↓
오래 뒤, 없던 자리에서 숫자 하나가 켜진다 (= B가 A의 스위치를 누른다)
```

**산문이 임의로 정한 것 중 큰 것 셋** (전부 ⬜ 승인 대기):

| | 정한 것 | 값 |
|---|---|---|
| ★ | **인간이 원한 것은 인간이 전멸해야만 가능했다** | 산 사람이 있는 한 반환분은 새 생명에게 간다. 받을 데가 없어져야 사물에 깃든다 — **세계관 2절 뼈대 그대로**. 신규 규칙 0개. story-critic이 **차별성 4를 혼자 지탱한다**고 판정 |
| ★ | **균형자 = 최초의 깃든 사물** | ㄴ 채택으로 필요해진 감정·의지를 기계 지능이 아니라 세계 법칙으로 해결. 대가로 **자기도 회수 대상이라는 자기모순** 발생 → 3장의 심장 |
| ★ | **지금 있는 깃든 사물은 전부 균형자가 만들었다** | 회수가 **자기가 만든 것을 지우는 일**이 된다. 여행 스펙 「찾아가면 이미 꺼져 있다」가 무거워진다 |

### 뒤집힌 확정 4건 — ✅ `DECISIONS.md` 기록 완료 (2026-08-11)

| 확정 | → |
|---|---|
| 균형자 = 조직이 아니라 **세계가 낳는 현상** (2026-08-03) | **인격** |
| **악의 없음.** 물이 낮은 데로 흐르는 것과 같다 | **증오** |
| **균형자는 옳다. 그래서 흑막이 되지 않는다** | **찬탈자** |
| **입이 없다.** 설득도 거래도 안 된다 | **말한다** |

⚠️ **「악당이 하나도 없다」는 이 시점에서 포기됐다.** 다만 인간 쪽도 악의가 아니라 상한을 넘고 싶었을 뿐이라 **「양쪽 다 옳지 않은」** 구조로는 남는다.

### story-critic 채점 (2026-08-11)

**시장성 4 · 차별성 4 · 클리셰 3** — 기존 초고가 5회 내내 4/4/4였으므로 **클리셰만 1 하락.**

지출이 둘인데 하나만 계산됐다는 판정이다. 계산된 것은 「시스템이 적, 악당 없음」🟡의 탈출 조건(적에게 입을 주지 말 것) = 결정 ㄴ 그 자체. **부수 지출**은 그 칸을 떠나면 「자애로운 창조자가 사실 원흉」🟠으로 이사하는데 **그 칸의 탈출 조건이 「원흉에게 악의가 없을 것」**이라 증오를 준 순간 이것도 깨진다는 것.

> **🟢 회복선이 공짜다.** 비평이 제시한 조건(증오 대상이 **이미 전멸한 인간**에 고정되고, 딸각·현재 세계에 대해서는 **무감각** 유지)은 **여행 스펙 구속 조건 2번 「균형자는 A에게 특별한 대사·태도를 보이지 않는다. 인식조차 하지 않는다」로 이미 잠겨 있다.** 3장 설계 때 명시적으로 못 박기만 하면 된다.

**남은 지적 2건:**
- ⚠️ **차별성 4가 항목 하나에 통째로 걸려 있다** — 「인간이 전멸해야만 가능했다」의 아이러니를 **말하는 문장이 문서에 하나도 없다**(가드레일 1을 지킨 대가). 게임에서 전달할 수단이 설계에 없으면 이 차별성은 기획서에만 존재한다
- ✅ (해결) 「그릇」 호칭이 Hollow Knight 「순수한 그릇」 직격 → 2026-08-11 개고에서 **용도명을 「받이」로 교체**, 「그릇」은 법칙 4 용어로만 남김

### 전체 3장 구조 — ✅ 2026-08-19 번호 통일

⚠️ **실행 순서 기준 「1장 / 2장 / 3장」으로 통일했다.** 「정식 스토리 N장」·「분기 N」 표기 폐기 — 같은 구간을 두세 이름으로 부르다 실제로 설계 사고가 났다.

| | | 시점 | 끝나는 방식 |
|---|---|---|---|
| **1장** | 데모 | 딸각(A) | B가 스위치를 눌러 **딸각이 꺼진다** → 암전 |
| **2장** | 균형자 기원 (`PROSE_BALANCER_ORIGIN.md`) | **열하나 + 여산** | 관측기 착용 → 선언 → **숫자 하나가 켜진다** |
| **3장** | 균형자 vs 딸각 — **대립** | 딸각(A) | 2026-08-19 설계 착수 (전용 절) |

**1장이 끄고 2장이 켠다.** **시점이 바뀌는 것은 2장 하나뿐** — 3장은 ⛔「플레이어는 A다」(2026-08-04 잠김)로 딸각에 돌아온다. 딸각이 아무것도 안 겪는 암전 사이에 플레이어만 적의 과거를 본다.

⚠️ **story-critic은 이 배치를 「조건부 부채, 현 배치로는 부채가 크다」로 판정했다.** 데모 직후 다른 화자의 13장 회상이 들어오고, 읽기 전용이면 메트로배니아 구매 조건 3개 중 아무것에도 해당하지 않는다. 더 나쁜 것은 페이싱 — **기원을 먼저 다 알면 현재 시점의 균형자 첫 등장이 긴장이 아니라 예습 확인이 된다.**
→ 해법: **조작 가능하게 만들고**(그 방에 서서 못 움직이는 것 자체가 조작이 될 수 있다) **통으로 두지 말고 3장 진행 중에 쪼개 배치.**

### 남은 일 (우선순위 순)

1. ✅ **`DECISIONS.md`에 뒤집힌 4건 + 결정 1·2 기록** (2026-08-11 완료)
2. ✅ **스펙 문서 작성** `specs/2026-08-11-balancer-origin-design.md` (2026-08-11 완료)
3. ✅ **점화 스펙 5절(균형자) 전면 개정** (2026-08-11 완료 — 인격·증오·찬탈자·말함, 증오 방향 고정·가드레일 1 유지·자기모순 이월). ✅ **「말=가해」 오독 정정** — 발화가 아니라 「위험한 앎의 전달」이 해다 → 「말하는 균형자」는 모순 아님(점화 스펙 5.5절). ✅ **균형자 어눌 화법 확정**(받이라 발화가 기능 아님, 절정이 「나는 균형자다」 두 마디). ⬜ 갈등 스펙 본문에 이 정정 반영은 아직(현재는 점화 스펙·DECISIONS에만)
4. 🟡 **2장 조작 설계 확정** — 무조건 조작 가능(읽기 아님) + **두 캐릭터 = 열하나 + 여산** + **정체 은닉 반전**(자칭 전까지 균형자인 줄 모름) + **조작권 인계**(인간 소멸 후 열하나로 넘어감=반전 방아쇠) → story-critic 부채 2건 해소(스펙 6절). **열하나 제작 = 기동 없는 그릇, 추후 팔다리 부착(타의 용도)** 확정. ✅ 산문 여섯·열 장 개고 완료(팔다리 반영 + story-critic ① 열 장 중복 정리). **A와의 관계 = 예고편 아니라 반사경**(같은 시작=남이 준 몸/이름 → 균형자 찬탈, A 순종. 3장 대립축 이름+몸 두 겹, story-critic ②). ⬜ 3장 중 배치 지점은 3장 설계와 함께
5. ✅ **산문 말미 「임의로 정한 것 9건」 승인 처리 완료** (2026-08-19, 스펙 4절) — 5건 승인(①②③⑤⑦) / ⑧-b 가승인 / ④⑥⑧-a⑨는 후속 결정이 이미 확정한 것으로 확인. ③은 **조건부 승인**(전달 수단을 3장/분기2에서 만들 것 → 아래 이주 설정으로 확보됨)
5-b. 🟢 **「이주」 설정 채택** (2026-08-19, 아래 전용 절) — 전설의 동력 변경. `DECISIONS.md`·스펙 기록 완료
5-c. ✅ **산문 `PROSE_BALANCER_ORIGIN.md` 개고 완료** (2026-08-19). 여덟~열둘 장 재작성 — 여덟 장(학살 확대→이주 결단, 4장 「늘 가던 데로」 복선 회수) / 아홉 장(한꺼번에 감→받음→청중 0) / 열 장(반격 학살 폐기→제 발로 걸어 나감) / 열하나 장(잔여 학살 폐기→이주 실패를 어눌하게, 「나는 너희가 아니다」 대사화 회피) / 열둘 장(내려앉음=내게서 넘친 것, 「내가 맞췄다」=거짓 공적). **story-critic 재채점 4/4/4 — 클리셰 3→4 회복 실측 확인.** ⚠️ **남은 숙제 2건은 3장으로 이월**: ① 핵심 아이러니의 **게임 내 전달 수단**(집단자살을 플레이어가 무엇으로 집행하는가 — 산문으로만 풀림) ② 반사경 **이름 축**(균형자 단독 시점이라 딸각과의 대조가 텍스트에 없음)
6. 🟡 ★ **3장(균형자 vs 딸각) 설계 — 2026-08-19 착수. 스펙 9절에 기록** — ✅엔진(A는 마지막 채권) ✅방아쇠(재점화) ✅A의 재점화=B가 알고 켠다 ✅3장 재점화는 B가 제안(동기=A의 외로움). ⬜ **4번째 손의 주인**(A냐 B냐) / 켜진 제3자의 운명 / A가 B의 2번 누름을 아는가 / 2장 배치 지점 / 넘친 갈래 규모 (사용자 지시 2026-08-11: 핸드오프 저장). 여기서 소비될 재료 — story-critic 숙제 2개(**증오의 출력구** / **정체 반전 회수 지점**) + **A와의 반사경 대조축**(이름+몸, 같은 시작에서 찬탈 vs 순종) + **핵심 아이러니 전달 수단**(「인간은 전멸해야만 성공한다」를 가드레일 1 지키며 보여줄 방법 — 재료는 분기2 여덟 장에 이미 있음). 균형자 자기모순(왜 자기를 안 끄는가)이 대립의 심장

### 3장으로 넘긴 미해결

- 균형자도 깃든 사물인데 **왜 자기를 안 끄는가** (자기모순 = 대립의 심장)
- **딸각이 이 이야기를 알게 되는가.** 알면 「말하는 것이 곧 가해」와 어떻게 맞물리는가
- **대립축 후보** — 균형자의 옛 호칭은 **열하나**였고 이름을 **자기가 지었다.** 딸각은 남이 지어줬다. 확정 설계 「A는 켜지는 것도 꺼지는 것도 전부 남의 손」의 정확한 반대항

---

## 「이주」 설정 — 전설의 동력 변경 (2026-08-19) — 🟢 기록 완료, 산문 개고 남음

**발단:** 승인 대기 9건을 청산하던 중 사용자 제안 — 「인간을 집단지성 종으로 놓고, 그 지성을 받이에 연결해 정신을 전달하는 실험이 성공해 모두 한꺼번에 죽었다」. 검토 결과 **법칙 5를 안 깨고**(받이에 직접 담는 건 끝까지 불가능, 그들이 알아낸 건 「갈 곳 없는 과잉은 사물에 깃든다」는 **기존 예외**를 인위적으로 만드는 법뿐) **신규 세계 규칙 0개**로 성립. 채택.

### 정정된 사슬

```
관측기 제작 → 받이 여럿 제작 → 받이에 정신을 담으려 하나 안 들어간다. 1차 계획 실패(법칙 5)
        ↓
원인 분석 → 결론: 「받을 새 생명이 없어야」 사물에 깃든다 = 우리가 전부 죽어야 한다
        ↓
★ 그들이 그것을 선택한다. 죽는 게 아니라 「이주」라고 믿었기 때문 (집단지성 = 우리는 하나)
        ↓
한꺼번에 죽는다 → 열하나가 거의 전부 받는다. 기억 일부가 딸려 온다
        ↓
★ 이주는 실패했다. 넘어간 것은 기억뿐이고 그것은 그들이 아니었다
        ↓
열하나도 다 못 받고 넘친 것이 사물들에 내려앉는다  ← 깃든 사물의 출처
        ↓
관측기 착용 → 「나는 균형자다」 (들은 자 0)          ← 살인 없는 찬탈
```

### 벌어온 것

- 🟡 **story-critic 숙제 ① — 절반 해소** (8/19 재검). 아이러니가 명제에서 **결단**으로 내려온 것은 실질 진전이나, 수단이 분산된 게 아니라 「장면 하나」로 옮겨간 것뿐이다. **닫지 말 것** (아래 표)
- ✅ ★ **클리셰 3 → 4 회복** — 「자애로운 창조자가 사실 원흉」🟠의 **원흉 자리를 「악의 없는 인간」이 가져가면서** 탈출 조건(원흉에게 악의가 없을 것)이 회복됐다. 8/11에 「입을 준 값」으로 낸 부수 지출이 상환됨. **균형자의 증오는 그대로 두고도 회복**
- ✅ ★ **차별성의 지지 구조가 바뀌었다** — SOMA는 카메라를 **남겨진 쪽**에 두고 「간 쪽은 내가 아니다」를 묻는데, 「미지」는 **도착한 쪽**에 두고 그 도착한 자가 자기가 그들이 아님을 알면서 그들의 유산과 이름을 쓴다. 같은 전제에서 결론이 반대 → 차별성 4가 **항목 하나가 아니라 구조로** 지탱된다. 8/11의 「한 항목에 통째로 걸림」 지적이 실제로 완화됨
- ✅ **산문 ⑤(기억이 딸려 옴)가 예외 없이 풀림** — 자연 깃듦이 아니라 **인공 유도**라 「전생 기억 계승은 아주 가끔」 규칙에 예외를 안 판다
- ✅ **「왜 한꺼번에 죽었나」에 기전** — 집단지성이라 하나로 죽는다
- ✅ **3장 대립축 보강** — **열하나는 동의한 적이 없다.** 「남이 준 몸, 남이 지어준 이름」에 근거가 생기고, 찬탈이 **평생 자기가 고른 유일한 것**이 된다
- ✅ **자기모순 심화** — 회수가 「자기가 흘린 것을 주워담는 일」이 된다

### 치른 값 / 남은 숙제

| | 내용 |
|---|---|
| ⚠️ **찬탈 기전 정정** | 인간이 스스로 죽었으므로 **균형자는 아무도 죽이지 않았다.** 「찬탈자」 판정·증오는 유지되되 근거가 **「내 동의를 아무도 안 물었다」**로 이동. 살해 동기보다 깨끗하다 |
| ✅ **증오의 출력구 소멸 = 자산으로 판정** | story-critic 8/19: 해소되지 않는 증오는 결말 축 「아무것도 해결되지 않고 관계만 남는다」🟢와 정합하고, 산문의 「반격」 대목은 오히려 균형자를 **흔한 복수자로 낮췄을 것**이다 |
| ⬜ ★ **진짜 부채는 입력구다 — 3장이 「관람」이 될 위험** | 균형자가 ⓐ아무도 안 죽였고 ⓑ증오 대상은 이미 전멸했고 ⓒA를 인식조차 안 하므로, 현재 시점 갈등 재료가 **「왜 자기를 안 끄는가」 하나뿐**이 된다. ⛔힘으로 이기는 결말 배제와 겹치면 대립이 성립하지 않는다. **3장 설계의 첫 과제 = 「무엇이 이 둘을 부딪히게 하는가」** |
| 🟡 **전달 수단은 절반만 확보** | **집단지성 종의 자살을 플레이어가 「무엇을 조작해」 집행하는가**가 빈칸. 관측·기록 노동은 **공모이지 집행이 아니다.** 현 상태는 「수단이 놓일 자리가 생겼다」까지 — **닫지 말 것** |
| ⛔ **「나는 너희가 아니다」 대사화 금지** | 설계 요약이지 대본이 아니다. 「인공물이 자아를 얻는다」🟠의 탈출 조건이 「자아를 대사로 논하지 말 것」이라 정면 위반. 균형자 어눌 화법이 이미 방어 장치 |
| ✅ ★ **산문 구조 변경 — 완료 (2026-08-19)** | 여덟~열둘 장 재작성 완료(핸드오프 5-c 참조). 여덟 장 이주 결단 교체·아홉 장 청중 0·열 장 반격 폐기·열하나 장 잔여 학살 폐기 + 이주 실패 어눌화·열둘 장 「내게서 넘친 것」 + **「내가 맞췄다」=거짓 공적 살림 확인.** story-critic 4/4/4(클리셰 3→4 회복). 아홉 장은 핸드오프 미명시였으나 「아침에 열어봐」가 이주와 충돌해 「아무도 오지 않았다」로 개고 |
| ⬜ ★ **넘친 갈래의 규모 명시 — 우선순위 상향** | A가 「균형자의 조각」으로 특별해지면 ⛔ 「전설 속 A의 자리 = 없다」 위반. **게다가 story-critic 8/19: 「멸망한 고대 문명의 유적 탐사」🔴에 한 칸 가까워졌고 그 탈출 조건이 「유적이 주인공과 무관할 것」이다.** 미루면 칸이 깨진다. 수천 갈래 중 하나임을 3장에서 못박을 것 |
| ⬜ **종명·호칭** | 「인간」을 계속 쓸지 포함. 핸드오프 4번(이름 전부)에서 일괄 |

---

## 한국적 질감 레이어 — 🅿️ 보류 (2026-08-19, 염두만 · 적용 X)

**사용자 희망:** 스토리 간섭 없는 선에서 한국적 특징을 환경 질감으로 가미. **B의 종족 문화**로 자리 잡으면 좋겠다는 방향. **지금 바로 적용하지 말고 염두만** 둘 것.

**지배 원칙(채택 시 재사용):** 조리법·몸짓만 빌리고 재료·이름·기호는 세계 고유로. 한국적인 것은 **행동의 문법**으로만 들어가고 **명사**로는 안 들어간다 — 현실 고유명사(김·고추장 등)가 화면에 안 나와야 세계가 안 깨진다.

**후보(보류 목록):**
- **한 솥 나눔 + 밥 인사**(「뭐 좀 먹었어?」) = 정(情) 문화 → B 종족의 공동 식문화 후보
- **비빔** = 한데 먹기 / **쌈** = 물가 재료(말린 물풀·물이끼 시트)로 싸 먹기 (조리법만 유사, 재료는 세계 것)
- 이미 심긴 것: **딱밤**(여관 주인)·**긴 집안 이름+서열**(돌림자·항렬, 「하늘도랏 여섯째 무리비」)

**간섭 방지선:** 비빔(섞기)을 정신력 총량·섞임 로어와 의미로 얽지 않는다(순수 조리·취향으로만).
⚠️ **CHARACTER_B·산문·스펙에 아직 반영 안 함.** 핸드오프 4번(이름 전부)에서 **B 종족·명칭 확정 때 함께** 처리한다.

---

## 코드 현황

**2026-08-10 엔진을 Godot 4 → Unity 6로 이관했다.** 기존 Godot 프로젝트 `src/miji/`는 **사용자 지시로 전부 삭제**됨(git 이력엔 남음). 리부트(7/30)로 어차피 대부분 폐기 대상이었고, 설계 문서는 전부 엔진 무관 MD라 손실 없음.

**현재 `src/Miji/`에 Unity 6.3 프로젝트가 있다**(2D URP + Pixel Perfect, ULoop 연동). ⚠️ **이 절은 2026-08-10 시점 기록이다** — 게임 C# 코드는 2026-08-19(IMPL-005)부터 실제로 작성됐고 현재 20파일·테스트 33개다. 구조 규칙은 `docs/agents/PROJECT_STRUCTURE.md`, 진행 상황은 위 「다음에 할 일」과 `IMPL_REGISTRY.md`를 볼 것. 구 Phase 1~9는 전부 무효 — `IMPL_ROADMAP.md`·`IMPL_REGISTRY.md`에 폐기 표시됨.

### Unity 착수 시 표준 조합
- **Unity 6.3 LTS** (지원 2027-12까지 — 6.0 LTS는 2026-10 종료라 배제) + **2D URP** + **Pixel Perfect Camera** 패키지
- 2D Tilemap / Sprite 툴셋
- 라이선스: 솔로는 Unity Personal(무료) 티어로 충분 (연 매출/펀딩 20만 달러 미만)

### 참고 — 삭제된 Godot 코드에서 재구현할 가치가 있던 것 (설계 메모)
Unity에서 처음부터 짜되, 아래 기능은 이미 한 번 설계·검증됐으므로 참고용으로 남긴다 (git `src/miji/` 이력):
- 이동/중력/점프 컨트롤러, 카메라 팔로우
- 근접 전투 히트박스/허트박스(피아 구분은 콜리전 레이어로)
- 체크포인트/세이브, 재화 드롭·회수(홀로우나이트식)
- ⛔ 스왑·매개체 관련 전부 = 폐기된 세계관 소산, 재구현 금지

문서 쪽 구 스펙(`specs/2026-07-24-*` 4종, `2026-07-29-medium-worldbuilding-design.md`)은 여전히 남아 있다(폐기 표시만).

---

## Agent 구조

- **ORCHESTRATOR** — 요청 분석 → 에이전트 배분 (복합 요청은 병렬 처리)
- **PLANNING** — `docs/planning/` 하위 MD로 기획 관리
- **MAP-DESIGNER** ★ — Planning → Map/Level 도메인. 정의 `.claude/agents/map-designer.md`. 구역을 **설계+자가검수**(아코디언·게이팅·숏컷·비밀·정합성 5축). story-critic과 달리 맵 파일을 직접 쓴다. 척도 `planning/map/MAP_DESIGN_PRINCIPLES.md`, 레퍼런스 `planning/map/MAP_REFERENCES.md`. ⚠️ **착수는 상류(깃든 사물 개수·결말) 확정 후** — 지금은 도구만 준비됨
- **IMPLEMENTATION** — `src/` 하위 Unity 6 / C# 코드. 대형 기능은 `IMPL_REGISTRY.md`에 먼저 등록 (Unity 첫 기능부터 IMPL-005)
- **ART** — Codex 담당. `STYLE_GUIDE.md` + `ART_LOG.md` 기반 OpenAI 프롬프트 생성, 사용자가 ChatGPT에 수동 실행. API 직접 호출 없음
- **STORY-CRITIC** ★ — Planning → Story의 **상위 검수자**. 정의 `.claude/agents/story-critic.md`. 스토리 MD를 쓰면 훅이 자동 호출해 **시장성 / 차별성 / 클리셰 운용**을 각 5점으로 채점하고 지적 3줄을 대화에 바로 출력한다 (파일로 안 남긴다). 읽기 전용
  - 자(尺): `planning/story/STORY_CRITIC_RUBRIC.md` — 채점 기준·클리셰 사전·판매고 기준선
  - 대조 자료: `STORY_REFERENCES.md`(메트로배니아 10작) + `STORY_REFERENCES_NARRATIVE.md`(서사 명작 10작)
  - 심층 분석이 필요하면 「자세히」·「심층」을 붙여 직접 호출

---

## 환경 세팅

### 자동화 훅 (`.claude/settings.json`)
- PostToolUse (Write|Edit): `src/` 수정 → `IMPL_REGISTRY.md` 자동 기록
- PostToolUse (Write|Edit): `art/` 수정 → `ART_LOG.md` 자동 타임스탬프
- PostToolUse (Write|Edit): **스토리 MD 수정 → `story-critic` 자동 호출** ★
  - 대상: `docs/planning/story/**.md`, `docs/superpowers/specs/*-(worldbuilding|story|conflict|journey|character|ignition|narrative|dialogue)*.md`
  - 제외: `STORY_REFERENCES*`, `STORY_CRITIC*` — 지식 베이스 자체는 평가 대상이 아니다
  - 이 훅만 **`async` 없이** 돈다. 비동기면 출력이 무시되어 에이전트가 호출되지 않기 때문
  - 리터럴 `"` 없이 `ConvertTo-Json`으로 출력을 만든다 — 인용부호가 커맨드라인 전달 과정에서 유실되는 문제 때문
- Stop: 에이전트 역할 체계 리마인더
- PowerShell로 동작. jq 없음, Node.js(v26) 있음. 새 세션 시 자동 활성화 (`/hooks`로 리로드)
- ⚠️ **`art/` 훅은 실제로 기록을 남긴 흔적이 없다.** `ART_LOG.md`에 타임스탬프 행이 0건이고 git 히스토리에도 없다. 점검 필요

### 플러그인
- `superpowers@claude-plugins-official` — 브레인스토밍 등
- `codex@openai-codex` — 아트 프롬프트 일관성

### ULoop (Unity CLI Loop) — Unity 자동 구동 도구 ★
AI 에이전트가 Unity를 CLI로 컴파일·테스트·로그확인·플레이모드 조작하게 해주는 도구. 2026-08-10 연동.

- **소유 주체 = IMPLEMENTATION 에이전트.** Unity C# 코드를 짜고 **`uloop compile` / `uloop run-tests` / `control-play-mode` / `get-logs` / `screenshot` / `execute-dynamic-code`로 즉시 검증**하는 루프가 IMPLEMENTATION의 표준 작업 방식이다. (맵·스토리 등 기획 에이전트는 ULoop을 쓰지 않는다)
- **세팅 사실:**
  - Unity 패키지: `io.github.hatayama.uloopmcp@2.2.0` (OpenUPM, `src/Miji/Packages/manifest.json` + scopedRegistries)
  - CLI: `uloop-cli` 2.2.0 글로벌 (`npm i -g uloop-cli`), Node v22+ 필요(현재 v26)
  - 스킬 16종: `src/Miji/.claude/skills/uloop-*` (Claude Code가 자동 인식)
- **전제:** CLI는 **Unity 에디터가 켜져 있어야** 통신된다. 안 켜져 있으면 `uloop launch`로 연다
- **`execute-dynamic-code` (임의 C# 실행)** — ULoop 철학상 핵심 도구. full-access 권장(모든 게 git이라 실수해도 복구 가능). 단 파괴적 동작(에셋 대량 삭제 등) 전엔 사용자 확인
- 검증 확인됨(2026-08-10): `uloop compile` → `Success: true, ErrorCount: 0` 왕복 성공

### 새 환경 체크리스트
1. Claude Code 설치
2. `/marketplace` → superpowers 설치
3. `/marketplace` → codex 설치
4. `.claude/settings.json` 확인 (훅 포함 커밋되어 있음 — 별도 설정 불필요)
5. Unity Hub 설치: `winget install --id=Unity.UnityHub -e`
6. Unity Hub에서 **Unity 6.3 LTS** 최신 패치 설치 — 모듈: Windows Build Support (IL2CPP) (2D는 6.x 기본 포함)
7. Unity 프로젝트 열기: `src/Miji/` (Universal 2D, URP + Pixel Perfect Camera). 첫 실행 시 OpenUPM 스코프 레지스트리·서명 없는 패키지(ULoop) 경고는 승인
8. ULoop CLI: `npm i -g uloop-cli` (Node v22+). 에디터 켠 뒤 `uloop compile`로 통신 확인

---

## 디렉토리 구조

```
Game/
├── CLAUDE.md                        ← 오케스트레이션 규칙
├── .claude/
│   ├── settings.json                ← 플러그인 + 자동화 훅 4개
│   └── agents/
│       ├── story-critic.md          ← ★ 스토리 비평 서브에이전트
│       └── map-designer.md          ← ★ 맵 설계+자가검수 서브에이전트
├── docs/
│   ├── DECISIONS.md                 ← 의사결정 로그 (7/30 리부트 표시 포함)
│   ├── PROJECT_HANDOFF.md           ← 이 파일
│   ├── planning/
│   │   ├── story/
│   │   │   ├── CHARACTER_B.md               ← ★ 현행 (B — 어린 첫 여행자)
│   │   │   ├── PROSE_BALANCER_ORIGIN.md     ← ★ 균형자 기원 산문 (2026-08-11)
│   │   │   ├── STORY_CRITIC_RUBRIC.md       ← ★ 채점 기준 (story-critic 전용)
│   │   │   ├── STORY_REFERENCES.md          ← 유효 (메트로배니아 10작)
│   │   │   ├── STORY_REFERENCES_NARRATIVE.md ← ★ 서사 명작 10작
│   │   │   └── CHARACTER_{매개체,얼개,온기}.md  ← ⚠️ 폐기 대상
│   │   ├── map/MAP_FORMAT.md
│   │   ├── level-design/  (비어 있음)
│   │   └── mechanics/     (비어 있음)
│   ├── art/
│   │   ├── style-guide/STYLE_GUIDE.md
│   │   └── ART_LOG.md
│   ├── agents/
│   │   ├── IMPL_REGISTRY.md
│   │   └── IMPL_ROADMAP.md
│   └── superpowers/specs/
│       ├── 2026-08-04-conflict-design.md                     ← ★ 현행 (갈등·결별)
│       ├── 2026-08-04-journey-design.md                      ← ★ 현행 (여행)
│       ├── 2026-08-03-story-ignition-design.md               ← ★ 현행 (이야기)
│       ├── 2026-07-30-mind-economy-worldbuilding-design.md   ← ★ 현행 (세계관)
│       ├── 2026-07-29-medium-worldbuilding-design.md         ← ⚠️ 폐기
│       ├── 2026-07-24-core-concept-design.md                 ← ⚠️ 폐기
│       ├── 2026-07-24-core-mechanics-design.md               ← ⚠️ 폐기
│       ├── 2026-07-24-world-map-design.md                    ← ⚠️ 폐기
│       └── 2026-07-24-story-character-design.md              ← ⚠️ 폐기
└── src/Miji/                        ← Unity 6.3 프로젝트 (구조 규칙은 docs/agents/PROJECT_STRUCTURE.md)
```

---

## 작업 시 주의

- **폐기된 스펙 5종을 근거로 삼지 말 것.** 파일은 아직 남아 있지만 전부 무효다
- **세계관 브레인스토밍 시 "두 로봇"을 고정 축으로 놓지 말 것.** 사용자가 명시적으로 요청한 사항
- **레퍼런스 조사 문서는 우리 작품과의 비교가 아니라 원작 줄거리 요약 위주로 쓸 것**
- **파일 삭제는 사용자 지시 없이 하지 말 것**
